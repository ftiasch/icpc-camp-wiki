A. Being Solarly Systematic
===========================

题目就是在一个三维的模空间里模拟行星碰撞，碰撞时间解模线性方程组算一下就行，每次让最早碰撞的多个行星合并即可。时间复杂度$O(n^3 \log V)$。

注意~~可能死于~~细节，例如某一维关于时间的系数为0，这将不增加模数的规模，或是某一维关于时间的系数与模数不互质，这将使模数增加的规模小于这一维的模数。

B - Delete This!
================

最终选框的边界一定能卡到一些点上，离散化一下行坐标，枚举最终选框的行边界，再two
pointer选列边界，计算贡献即可，注意中心在屏幕外的黑图标必须移动。时间复杂度$O((n+m)^3)$。

C - KenKen You Do It?
=====================

由于同一种数字不能在同行同列，所以预处理一种数字能占的局面，然后状压dp把每个数字填到可以填进去的位置即可。时间复杂度$O(n 3^m \sqrt{t})$。

~~有点卡常数，需要手写散列表。~~

D. Rings
========

裸题，从白格开始bfs即可。时间复杂度$O(nm)$。

E. Squawk Virus
===============

裸题，直接模拟。时间复杂度$O(mt)$。

F. Transportation Delegation
============================

每个运输公司拆点实现点流量限制，每个点可以向对应的运输公司连边，类似二分图建边求最大流即可。时间复杂度$O((r + f + \sum{n}) \sqrt{r + f + s})$。

G. Tray Bien
============

裸题，视作整点障碍然后直接轮廓线dp。时间复杂度$O(2^3 m)$。（如果读错题那就是数据太水了）

H. Trick Shot
=============

发现镜像对称的模型之后就简单了，按照题目要求的物理模型检查即可，注意母球打到1号球之前不能使2号球移动。时间复杂度$O(1)$。

I. What’s on the Grille?
========================

裸题，直接模拟。时间复杂度$O(n^2)$。
