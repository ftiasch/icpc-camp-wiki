A. Army Buddies
===============

两个方向分别用并查集维护即可。时间复杂度$O(B \log S)$。

B. Ball Stacking
================

转45°变成矩阵形式，利用预处理信息快速维护后缀最值并DP。时间复杂度$O(N^2)$。

C. Candy’s Candy
================

本质不同的解一定可以表示成(每组数量,
复杂组数量)，设每组的数量为$m \cdot n$，则每个$C_i$都可以表示成$k1 \cdot m + k2 \cdot m \cdot n$的形式。

当$C_i$不全相同时，有$m\cdot n | C_i - C_j$，即$m\cdot n | gcd(C_i - C_j) (1 \leq i, j \leq n)$，而当$C_i$完全相同时则有$m | C_i$，皆可枚举$m$，计算对应的复杂组数量。时间复杂度$O(F \log F + \sqrt{C})$。

D. Diccionario Portunol
=======================

难点在于`(前缀, 后缀)`的去重，实际上可以视为将匹配尽量放在前缀进行。

对于后缀长度大于1的情况，只需枚举前缀后继节点不包含的结尾字符作为后缀的开头即可。时间复杂度$O(26 \sum{|W|})$。

E. Electrical Pollution
=======================

虽然这并不是个二分图，但是给出的都是等式关系。根据$F(X) + F(Y) = A$可以将每个连通块里的任意一个$F(v)$用某个$F(u)$表示。

显然当产生多种表示时，只会无解或存在唯一解，注意输入格式到建边的转化。时间复杂度$O(Q)$。

F. File Retrieval
=================

考虑将后缀排序后一段$lcp \geq L$的后缀集合，当$L$变化时后缀集合可能产生变化，但只有$L$到了某两个相邻后缀的$lcp$或者某个后缀的长度时后缀集合才会变化。

因此答案的个数是$O(n)$的，可以枚举这样的$lcp$，二分出后缀区间，利用线段树求出后缀集合的状态，将所有可能的状态去重即可。时间复杂度$O(\sum{|W|} \log \sum{|W|})$。

G. Garden Fence
===============

任意一个不经过任何点的划分一定可以调整到直线与两个点十分相邻，因此只需枚举其中一个点，用两条相反方向的极角序扫描线走两圈即可。时间复杂度$O(P\log (P + L))$。

~~说得容易，实现较烦~~，注意常数。

H.Hedge Mazes
=============

路径唯一等价于只走桥边，求出桥后维护连通性即可。时间复杂度$O(R \alpha(R) + C + Q)$。

I. In Braille
=============

裸题，模拟。时间复杂度$O(D)$。

J. Jupiter Atacks!
==================

裸题，模数为质数，利用乘法逆元的知识发现树状数组维护一下就可以了。时间复杂度$O(N \log L)$。

K. King’s Poker
===============

裸题，分类讨论一下就行。时间复杂度$O(1)$。

L. Gates
========

看题解之前并没有想到这个结论。。

考虑将问题放到实数域上，每个点的输出值为输入的平均值向最近$\frac{1}{2}$的倍数取近似值，则这个取近似的过程具有一定随机性，可以测算出取到的上下界，具体做法为将每个点的输出值先取到极值，再将某个常数输入设定回实际值，不断迭代即可。

而这个问题乘2之后就是整数域上的问题了，可以做类似的迭代过程，而每个点的输出值最多产生2次变化（0与$\frac{1}{2}$之间，$\frac{1}{2}$与1之间），所以每个点最多作为输入值重新计算$O(2 M)$次，其中$M = \sum{k_i}$表示有向边的个数，因此只需要拿队列来模拟迭代过程即可，总会终止。时间复杂度$O(M)$。

M. Game
=======

如果存在石子放在编号为$(m-1)$的格子，那么这个石子以及与这个石子相邻的石子都可以一步移动到编号为$m$的格子，先手必胜。

否则先手不存在一步可以胜利的方案，为了防止对方获胜，每个人都会尽量不将石子放到编号为$(m-1)$的格子中。

该种情况下，如果某个人占据了$(m-n-1)$到$(m-2)$这$n$个格子，则另外一个人只能移动一个石子到编号为$(m-1)$的格子，从而前者可以胜利，于是只用考虑将$n$个石子移动到$(m-n-1)$到$(m-2)$这$n$个格子的问题，这转化为`移动完最后一个石子的人胜利`。

设从左往右第$i$个石子到$(m-2)$这个格子之间没有石子的格子数量为$f_i$，则$f_i$非降，而且每次操作只能使一段连续相同的$f_i$减1。

设$c_i$表示$f_j=i$的数量，每次操作相当于选择两个正整数$i$和$x$，满足$x \in (0, c_i]$，使得$c_i$减少$x$，$c_{i-1}$增加$x$，而将$c_0$之外的$c_i$恰好全变为0的一方胜利，所以获胜方的最后操作必然是关于$c_1$的，而且每次操作只和$i$的奇偶性有关系。

考虑奇数$i$情况的$c_i$异或和作为$SG$值，$SG$不为0则存在先手必胜方案，先手对奇数$c_i$的操作必胜需要满足$SG\oplus c_i \in [0, c_i)$，先手对于偶数$c_i$的操作必胜需要满足$(SG\oplus c_{i-1})-c_{i-1} \in (0,c_i]$。时间复杂度$O(n)$。
