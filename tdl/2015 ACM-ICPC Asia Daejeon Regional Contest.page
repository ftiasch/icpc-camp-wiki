A. Coin Swap \[Skywalkert\]
===========================

bfs
出每个硬币到可以到达的节点的距离，将硬币和节点进行最小权完美匹配即可。

B. Equilibrium State
====================

C. Grid \[Skywalkert\]
======================

总操作次数尽量少，可以看作是能够影响两个数的操作尽量多。

建立二分图模型，每个点有点流量上限（源到这个点或这个点到汇的流量限制），相邻点之间的边流量上限是二者点流量上限最小值，最大流跑一跑就行了。

将线性规划的式子对偶后也可以得到这个结果，因为二者原理相同，注意对偶是不需要考虑解集所在的空间是怎样的。

D. Kernel
=========

E. Log Jumping \[Skywalkert\]
=============================

把这个环形序列改成一段上升一段下降不会使解变差，也即找两条从最小值到最大值的路径涵盖所有的点，设非降序排序后的序列为$A$，不难发现答案就是$\max{A_{i + 2} - A_{i}}$。

F. Odd Cycle \[constroy\]
=========================

给一个有向图，找出其中任意一个奇环。 从任意点开始 dfs，
黑白交替染色，dfs 路径上的点标记为 +1， 搜索完成的点标记为 -1。\
若一个点的两种颜色都出现在 dfs
路径上，则发现一个奇环，沿着前趋找回去输出答案。\
显然每个点最多经过 2 次，黑白各一次，复杂度 O(n + m)。\
（有人用 Tarjan 判奇偶也过了，我表示不能理解）

G. Path
=======

嘴巴题解: 给定的多边形是一个 x
方向的单调多边形，可以方便的做三角剖分，然后用奇怪的方法搞一搞就可以过了。

H. Polynomial \[Skywalkert\]
============================

设$x^{\underline{k}} = \prod_{i = 0}^{k - 1}{(x - i)}$，则$\sum_{0 \leq x \leq n}{x^{\underline{k}}} = \frac{(n + 1)^{\underline{k + 1}}}{k + 1}$。

由于给出的多项式的度数$deg$不超过 25
，所以拿高精度暴力积个分就行了，为了避免分数，可以先乘上$(deg + 1)!$。

I. Stock \[braveTester\]
========================

设有最优策略 $P$。考虑价格最高，且天数 $x$
最靠后的股票，如果这天的决策是买，由于 $x$
的性质，卖了一定亏，所以改成什么都不做一定不会使解变得更差。如果这天的决策使什么都不做，由于
$x$ 的最大性，改成卖掉手上有的所有股票肯定不会变差。如果有 $y < x$，且
$y$ 在 $x$ 之前被卖掉了或者什么都没做，那么改在 $x$ 卖解不会变得更差。

上述几条性质可以得出如下做法：对价格按照从高到低，天数从大到小排序，从左往右依次判断当前价格是否有效，如果有效，那么将还没处理的天数比它小的股票全部买入，在这天卖掉，否则什么都不做。复杂度
$O(n\log n + n)$

J. 3-Primes Problem \[Skywalkert\]
==================================

拿素数做 3 轮背包即可。

K. Tree Edit
============

L. Wheel of Numbers \[braveTester\]
===================================

由于 $n \le 100$ 且 $m \le 9$，因此得到的每个数都不超过
int，所以暴力模拟就好。

Summary
=======

**Constroy:**

**Skywalkert:**
题目画风一脸懵逼。对自己的技能掌握不够熟练，常出现怀疑自己的情况，今后要尽量在训练和比赛中消除这种问题。

**braveTester:** WA
了发签到，过了另一个签到。一个几何有了灵感但是不会写，稀疏矩阵不会消元。为唐老师助攻了一发。在多校出过的
A 还是没掌握。码力，码力，码力。
