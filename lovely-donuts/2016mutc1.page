1001. Abandoned country \[ruclion\]
-----------------------------------

水题，边长均不相等，求最小生成树+树dp算每条边两边有几个点

1002.Chess \[ruclion\]
----------------------

水题，求出每行的sg值异或起来。注意每行枚举下一步状态的顺序。

1003.Game \[skyxuan\]
---------------------

发现图中任意两点都是联通的，他们之间的距离如果不考虑障碍的话就是曼哈顿距离。之后再考虑障碍对那些点对之间的距离会有影响，如果有影响的话只会使这些点对之间的距离加二，分四个方向来枚举。

1004.GCD \[skyxuan\]
--------------------

以每个点为左端点，gcd不同的右端点只有$\log n$个，找出所有的这样的区间就好了。

1005.Necklace \[ruclion\]
-------------------------

法一：将1号黑球断开，然后是一个链上的状压dp。
法二：阶乘枚举白球的位置，黑球和空匹配，匈牙利。这道题不能直接网络流，因为不能控制很多个小环。

1006.PowMod \[ruclion\]
-----------------------

考虑由于$\varphi (i)$是积性函数，因此我们对于每个质因子求出其贡献即可。
考虑$n$的某个质因子$p$,我们设$sum(n,m)=\sum_{i=1}^{m} \varphi (i*n)$.那么考虑将质因子$p$的贡献单独处
理，可以得到$\sum_{i=1}^{m}\varphi (i*n) = \varphi (p) * \sum _{i=1}^{m} \varphi (i* \frac{n}{p}) + \sum_{i=1}^{m/p} \varphi (i*n)$.
因此可以利用$sum(n,m) = \varphi (p) * sum(\frac {n}{p},m) + sum(n,\frac{m}{p})$来递归地计算k.
最后可以利用$a^{b}\%p = a^{\varphi (p) + b\%\varphi (p)}$,递归地计算$k^{k^{k^{...^{k}}}}$

1007.Rigid Frameworks \[ruclion\]
---------------------------------

两步，首先是如何能够稳定。固定一个能让该行和该列的边垂直，那么要任意两个行列均垂直，就是一个联通的二分图。第二步是算有多少个联通的二分图，用f，g倒着弄就好。

1008.Shell Necklace \[skyxuan\]
-------------------------------

$f[i] = \sum\limits_{j = 0}^{i - 1}f[j] \times a[i - j]$
如果公式里面是两个和f无关的函数，就直接是FFT了，但是里面和f有关，就需要套一下cdq分治。

注意因为模数是313，它减一不是一个二的幂次，不能用NTT。因为模数非常小，每次直接FFT，然后取模就行了。

1009.Solid Dominoes Tilings \[ruclion\]
---------------------------------------

容斥，行和列要同时容斥，枚举列的分割情况，用第一次出现分割的位置递推行来容斥。预处理是常规的轮廓线dp

1010.Subway
-----------

1011.tetrahedron \[ruclion\]
----------------------------

几何，计算法向量，算出平面的方程，枚举方程的相等情况，高斯消元解方程，最后判定在四面体内
