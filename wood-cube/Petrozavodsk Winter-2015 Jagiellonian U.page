A. Ladder
---------

B. 36 Puzzle
------------

C. Message
----------

D. Arithmetic Sequences
-----------------------

E. Subsequence \[TDJ\]
----------------------

首先CDQ分治，因为DP是从左往右转移所以需要预处理好归并树，然后对于两边的点按y坐标从小到大依次加入，分别维护两个栈（对左半而言即当前右上无其他点的节点，右半类似），同时再将两个栈的信息（左半提供值，右半视为隔板）插在一起维护一个链表实现的单调队列，操作由栈操作触发，分别有有插入值、删除最靠上值、插入隔板、删除最靠上的隔板，询问当前最高隔板上的最小值，即可在O(N)时间内完成单次分治，总复杂度O(N\*lgN)。

F. Algebra is Awesome \[TDJ\]
-----------------------------

按顺序对每个环在所有合法偏移量（即跟之前环偏移量的同余方程不冲突 且
跟本环长互质）中找一个最小表示（最小数字的后一步最小）即可。

G. Bus Lines
------------

H. Coal Mine
------------

I. Chocolate is Tasty \[TDJ\]
-----------------------------

按长边与短边的差做折线，男生+1，女生-1，可以用跳表维护一段折线的最低点与左右端的相对高度，对于给定的初始长宽即可通过计算最后的差来求出长宽，复杂度O(N\*lgN)；此外，用单调队列维护折线最低点并双指针扫可以做到O(N)。

J. Game
-------

K. Tic-tac-toe
--------------
