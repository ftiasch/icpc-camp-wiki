Contest Info
============

date: 2017.08.14 - 2017.08.29

[practice link](https://vjudge.net/contest/178833)

Solutions
=========

A. Tree Construction
--------------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

$f[i][j]$ 表示合并区间 $[i,j]$
的最小代价，$f[i][j]=min(f[i][k]+f[k][j]+cost(i,k,j)$
发现满足四边形不等式，于是复杂度 $O(n^2)$。

B. Mice and Holes
-----------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

$f[i][j]$ 表示前 $j$ 个老鼠跑到前 $i$ 个洞的最小代价，设 $f[i][j]$ 从
$f[i-1][k]$ 转移过来，那么转移具有单调性，即随着 $i$ 递增，$k$ 也递增。

C. Andryusha and Nervous Barriers
---------------------------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

从高往低做，设同横坐标同高度掉落的球形成一个集合，每个挡板最多会生成两个新的集合，并至少消去一个新的集合，那么最多出现过
$n+w$ 个集合。

用线段树维护一个横坐标区间集合内高度最低的集合，从高往低枚举挡板，用线段树找出所有被它挡住的集合，然后将新生成的集合加进线段树，复杂度
$O((n+w)\log w)$

D. Snakes and Ladders
---------------------

upsolved by skywalkert

------------------------------------------------------------------------

skywalkert's solution

转移存在环，不存在无解情况，高斯消元直接求解。

E. Ciel and Gondolas
--------------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

$f[i][j]$ 表示前 $j$ 个分成 $i$ 段的最小代价，那么
$f[i][j]=\min(f[i][k]+cost[k+1][j])$ ，满足决策单调。

F. 吉哥系列故事——恨7不成妻
--------------------------

upsolved by skywalkert

------------------------------------------------------------------------

skywalkert's solution

记录不使用数字 $7$ 的长度为 $i$、数位之和模 $7$ 为 $j$、数字模 $7$ 为
$k$ 的数字的 $0, 1, 2$ 次方和，数位 dp 求解。

G. Singer House
---------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

设 $f[i][j]$ 表示 $i$ 层 $j$ 条路径的方案数，实际上 $j$ 最多只有
$n$，$f[i][l]$ 和 $f[i][r]$ 可以转移到 $f[i][l+r]$ 和 $f[i][l+r-1]$ 。

H. Karen and Supermarket
------------------------

solved by skywalkert

------------------------------------------------------------------------

skywalkert's solution

根据当前子树使用或不使用优惠进行 dp，复杂度相当于在 lca 处枚举子树节点，
$\mathcal{O}(n^2)$。

I. Bear and Company
-------------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

$f[i][j][k][0/1/2]$ 表示前 $i$ 个 $'V'$ 和 前 $j$ 个 $'K'$ 以及前 $k$
个其他字母拼在一起，且最后一个字母是哪个的最小代价，枚举下一个放什么字母，转移即可，复杂度
$O(n^4)$

J. Send the Fool Further! (medium)
----------------------------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

$dp[i]$ 表示从 $i$ 出发向子树走回到 $i$ 的最大花费，$ddp[i]$ 表示从 $i$
出发向子树走最终不回到 $i$ 的最大花费，计算 $ddp[i]$
的时候，枚举向哪个子树走不回头即可。

K. Bash Plays with Functions
----------------------------

solved by skywalkert

------------------------------------------------------------------------

skywalkert's solution

根据积性函数性质得到每个质数次幂的函数值为组合数，直接质因数分解计算即可。

L. GT and strings
-----------------

solved by chitanda

------------------------------------------------------------------------

chitanda's solution

子串用后缀自动机做，子序列则枚举较短的那个串的每一位，复杂度是
$O(26L+L\log n+L\sqrt{L})$。
