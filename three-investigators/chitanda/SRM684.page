SRM684 Easy
-----------

> 给定一个集合 $a$ 和一个数 $k$ ，问能满足下列条件的最大的 $a$
> 的子集的大小：将子集中的数两两之间的绝对值加入一个新的集合 $b$
> ，对于任意两个 $b$ 中元素 $A,B$ ，都有 $A\le k\times B$ 成立。

设子集中两两之间绝对值的最大值为 $x$ ，最小值为 $y$ ，那么只需满足
$x\le k\times y$
即可。将所有数从小到大排序后，枚举子集中最小的数和最大的数，相当于固定了
$x$ ，那么我们需要使得 $y$ 大于一个值，贪心的选取中间的数即可。
