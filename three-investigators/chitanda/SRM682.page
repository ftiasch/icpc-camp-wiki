SRM682 Easy
-----------

> 给出一个 $n\le 2000$ 个点的连通图，有 $m\le 2000$
> 条边，问是否能找到一个包含五个不同点的链。

用 bitset
维护每条边的端点连接到了哪些点，枚举两条没有公共点的边，看它们的端点是否能连接到同一个点，这样复杂度是
$O(m^2\times n/64)$ 的。

而标解的做法是，枚举两条没有公共点的边，如果这两条边之间的某对端点相连了，枚举第一条边的另一个端点所连的点，如果有除了这四个点之外的点，即有解。由于枚举端点最多枚举
$4$ 次，所以复杂度是 $O(m^2)$ 的。

还有一种更巧妙的做法，考虑如果度数为 $1$ 的点，如果有不小于 $n-2$
个，那么肯定无解，如果有 $n-3$ 个，那么看是否有两个度为 $2$
的点且三个度数不为 $1$
的点是否构成三元环，如果是无解，否则有解。除此之外，其他情况都是有解。

SRM682 Medium
-------------

> 给一个环套树，每次可以缩两个相连的点，问最少次数使得度数大于 $1$
> 的点的个数不超过 $1$ （重边算一条边）。

首先环上的点肯定缩成只有两个点，并且尽量保留一个初始没有与环上的点之外的点相连的点不与其他点缩。然后变成一棵树，树上度数大于
$1$ 的点的个数减 $1$ 即答案。
