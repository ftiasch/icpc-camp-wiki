## SRM702 Easy

> 给定一个 $n\times m$ 的矩阵，里面的值为 $1$ 到 $n\times m$ 的排列，可以任意交换两行或者两列，交换完后将所有数字依次列出，如果第 $i$ 个数为 $i$ ，那么该位为 $1$ 否则为 $0$ ，这样构成一个长为 $n\times m$ 的二进制数，问这个二进制数最大多少。

按照二进制从高位往低位贪心，即从 $1$ 开始贪心即可。

## SRM702 Medium

> 一个合法的括号序列定义如下：
>
> * $()$ 是一个合法的括号序列
> * 如果 $S$ 是一个合法的括号序列，那么任意正整数个 $S$ 组成的 $(S\cdots S)$ 也是一个合法的括号序列
>
> 给定一个括号序列，问它的合法的括号子序列中，字典序第 $k$ 小的。

根据它合法括号序列的定义，合法的括号子序列不会太多，爆搜出所有的即可。