## SRM694 Easy

> 给出$n(n\le 50)$个整数,每个数的范围为$[0,256)$,要将这些数分为三组,使得每组数的异或和之和最大.

直接DP,$dp[i][j][k]$表示分配了前$i$个,第一组的值为$j$,第二组的值为$k$时,第三组的最大值.

## SRM694 Medium

> $N$个人回答$M$个问题,问有多少个问题集合的子集,满足任意两个人对这个子集的问题的答案,不完全相同.

枚举两个人,得到他们答案相同的问题的集合,那么对于问题集合,不满足条件.如果一个集合不满足条件,那么所有它的子集都不满足条件,最后把集合按大小从大到小for一遍就能得到答案.

## SRM694 Hard

> 有 $N$ 个数,标号从 $0$ 到 $N-1$，$M$ 个操作,每次操作要将 $L_i$ 到 $R_i$ 的数都加上 $V_i$，这个 $V_i$ 的值必须在 $X_i$ 到 $Y_i$ 之间，使最后所有数的值相同，问这个最大的相同值。

二分答案 $d$，然后对每个操作，从点 $L_i$ 向 $R_i+1$ 连一条流量下界为 $X_i$，上界为 $Y_i$ 的边，从点 $N$ 向点 $0$ 连一条下界 $d$ 上界正无穷的边，判断是否有可行流。