[Contest Link](http://codeforces.com/gym/100343)

A. Kingdom Division \[Nero\]
============================

题意：给平面上n(&lt;=
100)个点，找一根从极左经过某些点连到极右的折线，把平面分成三部分(在折线上，折线上方，折线下方)，使得三部分点集大小的极差最小。折线在任意x只有一个函数值。打印路径。

题解：dp(i,j,k)表示从左边连到点i，第一部分与第二部分差值为j，第二部分与第三部分差值为k
是否可行。转移O(n)，复杂度O(n \^ 4)。需要O(n \^ 3)预处理转移。

B. Financial Software \[Nero\]
==============================

题意：给一个序列长度n(&lt;= 1e5)，数值范围 &lt;= 1e9，给定一个K(&lt;=
1e9)，找一个最长的子序列 使得子序列相邻两项的绝对值差不小于K。打印路径。

题解：dp(i)表示以i为右端点的最长子序列长度，拿两个树状数组来转移。需要先离散化。

C. Late Again \[Nero\]
======================

题意：给n(&lt;= 1000)点m(&lt;=
20000)边的无向图，有边权。指定了一条路径。调整每条边的边权使得指定路径是最短路。要使得边权最大变化最小。打印路径。

题解：二分变化值，那么路径上都减，非路径边都加，判一下最短路。

D.
==

E.
==

F. Substring Search \[JayYe\]
=============================

题意： 给你串A,B ( &lt;=
1e5)，问有多少个A的子串可以满足字母重新映射后等于串B。

题解： 哈希搞搞搞就可以了，细节要想清楚再敲，不然容易在机子上卡住。

G.
==

H. Truth is in the... \[JayYe\]
===============================

题意： 有m个问题，每个问题有三种可能，1：下一个问题是对的
2：下一个问题是错的 3： 正确的问题一共有n种
，要你构造出一个对错序列使得所有的问题都是对的。(有点绕。。

题解： 对于第三种问题作为分界，然后对于所有的链搞一下就好了。

I. Video on Demand \[Nero\]
===========================

题意：给出播放器的缓冲机制和等待的最长时间，找到使得停顿次数最少的缓冲区大小。多解输出最小那个。

题解：推一推式子得到最少停顿次数为$\lceil\frac{t * (b - d)}{p * d}\rceil$，然后二分缓冲区大小或者直接算都行。

J. Wheels within Wheels \[Nero\]
================================

题意：tarjan老爷爷有很多隐藏的papers没有被科普。其中包括：强连通图一定能够被拆解成
形如一个环上有很多强连通分量的形式。所以让你拆一下。n &lt;= 200,m &lt;=
300。

题解：既然说一定有，那么对于一个强连通图，肯定存在一条边，把它删掉后得到一条链，链上全是强连通分量。所以枚举删哪条，强连通分量递归做。O(m\^3)。
