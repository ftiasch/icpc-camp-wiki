[Contest Link](http://codeforces.com/contest/603)

A.
==

题意：给长度为n(&lt;=
1e5)的01串，恰好把一个区间的01取反。问最长的01交替子序列长度。

题解：dp(i,j,k)前i个字符，最后一个是j，当前区间翻转状态为k(0,1,2)的最优解。

B.
==

题意：给定质数p和一个K，问有多少函数满足f(Kx) = K \* f(x) mod p。

题解：考虑参数关联性，拿个并查集搞搞即可。特殊处理K = 0以及K = 1的情况。

C.
==

题意：双人博弈，n(&lt;= 1e5)堆石子，每堆ai &lt;= 1e9，给定参数K(1 &lt;=
K &lt;=
1e9)。交替操作。要么从一堆石子中删掉一个石子，要么把一堆数量为(2x)的石子拆成K堆数量为x的石子。问谁赢。

题解：容易发现只要处理K为奇/偶的情况。然后分别观察sg值，找到规律就做好了。复杂度O(nlogA)。

D.
==

题意：给定n(&lt;=
2000)个直线参数方程，无共线无平行。问有多少个三元组(i,j,k)三条直线的交点的外接圆经过原点。

题解：不会做QwQ

E.
==

题意：初始有n(&lt;= 1e5)个点，m(&lt;=
3e5)次操作。每次操作为增加一条边(a,b,c)，然后询问
从之前的边们之中选出一个子集，使得所有的点度都为奇数，子集内最大边权最小是多少。

题解：首先有一个结论
偶数个点的连通块必然可以选出一些边使得所有的点度都为奇数，这个不难证明。然后lct维护最小生成森林即可。当所有连通块点数都为偶数时，要考虑把当前的最大边删掉会不会导致奇数连通块出现，所以还要求有根子树的节点数。这个需要用lct维护有根树的虚节点和。
