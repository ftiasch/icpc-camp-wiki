[Contest Link](http://codeforces.com/contest/598)

A.
==

水

B.
==

水

C.
==

题意：给n(&lt;=10000)个向量，求两个夹角最小的。

题解：极角排序一下，相邻两个角度减一减即可。long double +
atan2大法好。注意余弦定理挂精度。

D.
==

水bfs

E.
==

题意：给一个 n(&lt;= 30) \* m(&lt;= 30)
的巧克力，可以横切/竖切/吃掉一块，切的话代价为长度的平方。问吃到K(&lt;=
50)小块的最小代价。

题解：暴力dp(n,m,K)，转移枚举切哪里枚举左边吃多少，就好了。

F.
==

题意：给一个n(&lt;=1000)点多边形(可能是凹的)，给m(&lt;=
100)条直线，问每条直线与多边形相交部分的长度。交在边上也算。

题解：把多边形与直线相交的位置全都搞出来，使用射线法判断当前线段在不在多边形内部/边界上，在的话统计到答案里。这样比三角剖分好写不知道哪里去了。需要long
double,出题人就一long double 狂魔。eps设1e-5,1e-6,1e-7,1e-8都能过。
