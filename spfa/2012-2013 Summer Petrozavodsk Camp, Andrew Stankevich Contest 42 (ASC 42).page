A. Abstract Dances \[\*JayYe\]
==============================

题意：
定义有趣的舞蹈是一个movement，定义movement是由若干个figure构成的，用逗号分隔。定义figure是由左右括号中间有若干个movement构成，movement中间用感叹号隔开。对于两个不同的movement，用逗号隔开的那些figure循环平移后如果完全相同，那么这两个movement是等同的。对于两个不同的figure，用感叹号隔开的movement如果存在一个下标的映射使得前一个movement能够和后一个movement完全相同，那么视为等同的fugure。
要求出n &lt;= 60个括号对的不等同的有趣的舞蹈的个数。

题解：
对于figure，由于是映射后相同视为等同，那么可以根据movement的大小从小到大dp就可以了。对于movement，循环后可能会相同，很显然，用polya定理就可以去重了。然后就是写写写，答案非常大，最后套上大数打个表，可能复杂度比较高，打表打了好几分钟。

B. Betting Fast \[Nero\]
========================

题意：有个人带了s块钱去赌场赌钱，赌至多t轮，要赚够到n(&lt;=
1e5)块钱。每次赌赢的概率为p(&lt;= 0.5)，求最大期望。

题解：一个公平的赌场，怎么压都会是一样的。所以每次压尽量多的钱，然后就做好了。

C. Comparator Networks \[JayYe\]
================================

题意： 给一个n( n &lt;= 10
)长度的01串，求是否存在一种交换排序序列使得所有01串除了它本身，都能被变成有序，如果有输出解。

题解：
爆搜，对于所有交换如果不改变当前串，并且不会把其他串变成全等于当前串，那就交换，然后这样贪心过了=
=950+ms

D. Dynamic LCA \[JayYe\]
========================

题意：
给n个点的一棵树，m个操作，有两种操作，一种是换根，一种是给两个点求lca。n
&lt;= 1e5, m &lt;= 2e5

题解： 求两个点和根两两之间的lca，深度最大的lca就是答案。

E. Equal Hash Codes \[\*Nero\]
==============================

题意：有一棵树，定义$hash(u) = 1 + \sum{hash(vi) * t^i}$，vi为u的子节点按照hash多项式排序后从小到大第i(1-base)个儿子。现在给定一个hash多项式，找出所有hash值是这个多项式的n(&lt;=18)个节点的树的括号序列，按字典序输出。

题解：观察可以知道：1.多项式系数和等于n。2.多项式中间项的系数不能为0。3.多项式的最高次项必然来自其最右边的儿子。4.多项式的i次项系数不多于i+1。所以每次枚举最右边的儿子长什么样以及有几个儿子，然后大力搜。。

F. Funny Game \[JayYe\]
=======================

题意： 给n \*
m的蛋糕，先手只能横切，后手只能竖切，不能切的，并且必须平均切，问谁赢。

题解： n的质因子个数大于m的质因子个数的话先手赢，否则后手赢。

G. Grand Tour \[Nero\]
======================

题意：给定有向无环图(n &lt;=
1e5)，找到一个拓扑序A，使得$max\{0,max\{i - D_{A_i}\}\}$最小。

题解：把图反过来优先队列拓扑序，D值大的先弹出。

H.
==

I.
==

J. Journey Planner \[\*Nero\]
=============================

题意：给一有向无环图(n &lt;=
1e5)，设定每个点的权值为它能到达的点的数量。找到一个点，使得不存在另一个点比它权值的两倍还大。

题解：考虑找一个有向森林，使得每个点在森林里的权值不小于有向图里的一半。至于如何构造这个森林，猜的结论：按照每个点的出度从大到小与它的没有父亲的临点连接。比赛时写逗了下标，没有细查，强行漏题...(到底是数据水呢还是结论对呢(雾))。
