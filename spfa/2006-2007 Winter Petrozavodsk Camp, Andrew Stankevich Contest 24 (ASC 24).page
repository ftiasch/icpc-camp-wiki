[Contest Link](http://codeforces.com/gym/100340)

A Cookies \[JayYe\]
===================

题意：
有n个小孩，m个糖，每个小孩有个贪婪值g\[i\]，现在把糖分给每个小孩使得所有小孩都有糖，每个人有不开心值，假设有a\[i\]个人的糖比第i个小孩多，第i个小孩的不开心值为a\[i\]
\* g\[i\]，要使得所有的不开心值加起来最小，求方案，输出来。n &lt;= 30, m
&lt;= 5000

题解：
首先对于g\[i\]排序，g\[i\]越小给的糖越少。然后dp是一层一层铺的，设dp\[i\]\[j\]表示总共给了j个糖，并且i到n这些人的糖果数当前是相等的。接下来需要枚举下接下来铺多少个，后面再完全背包下就可以了。复杂度为O(n\^2
\* m)

B
=

C. Epig \[Nero\]
================

题意：从前，有n个client，第一个client要把一个文件传给大家，它把文件拆了K份。同一个时刻一台client只能传一份和接受一份。然后题面描述了详细的传/接规则。问每个client收到完整文件的时间。

题解：题意题，照着一句一句模拟即可。

D. Irreducible Young Diagrams \[Nero\]
======================================

题意：有一个非上升序列，把每个数看成一行紧贴左上侧的ai个格子。每次操作可以把两个连续的格子删掉(不能破坏原来的性质)。删到不能删为止，问最后有多少种形状以及都长什么样。

题解：从过题人数上看，形状只有1种。那么不管按照什么决策删都殊途同归，所以从后往前删即可。

E Permutation Reconstruction \[JayYe\]
======================================

题意：
对于一个排列，删掉一个数后然后令比这个数大的数减一，可以得到另一个排列。现在告诉你排列大小是n，告诉你删掉每个数后得到的排列，排列是乱序的，要求还原排列，保证有解。
n &lt;= 300

题解： 从小到大放数字，然后再枚举每个位置判断是否可行，复杂度为O(n\^3)。

F Decoding Prefix Codes \[JayYe\]
=================================

题意：
给你一个明文和密文，明文是字母，密文是01串，对于每个字母对应一个01串，并且不同字母的01串没有前缀关系，要求还原每个字母的01串。
条件，每个01串的长度不超过10，不同字母的个数不超过10。

题解： 直接搜，最坏复杂度是10\^10，然而仔细想想肯定到不了，限制太多了。

G Hungry Queen \[\*JayYe\]
==========================

题意：
在二维坐标系下，有个皇后在(0,0)，皇后左右移动或者向上移动或者对角线向上移动，并且碰到了小兵一定要吃，告诉你n个小兵的位置，皇后想要吃尽量多的小兵，求最多个数，并且输出解。

题解：
由于只能向上移动，dp即可，还有横向移动的特殊处理下。然后比赛中太暴力了，用各种map
MLE了，也没什么时间改了，赛后优化空间set之类的搞搞过了。

H. Boat Race \[Bobo\] \[\*Nero\]
================================

题意：从前有一条等宽的河，它的南岸(x0,y0),(x1,y1)...(xn,yn)，x单调递增。河宽度为w。问从河西侧赛艇到东侧的最短距离。

题解：容易知，事件点只在端点和东西侧上，所以枚举两个端点判断能不能直连。先取线段中点看在不在多边形内，然后判断线段和其他线段没有交点即可。东西侧特判。

I. Longest Common Subpair \[Nero\]
==================================

题意：给两个串X和Y，找两个长度和最长的子串a和b，使得X = x1ax2bx3,Y =
y1ay2by3。长度不大于3000。

题解：枚举(i,j)算X(0,i)与Y(0,j)的最长公共子串，和X(i + 1,n - 1)与Y(j +
1,m - 1)的最长公共前缀并一下即可。具体使用了哈希。注意本题卡n\^2空间。

J. New Year Tree Transportation \[Nero\]
========================================

题意：给一棵树(n &lt;= 1e5)，把它分成不多于(n + n + K - 1) /
K个连通块，使得每块size不超过K。输出方案。

题解：dfs一遍，从下往上贪心切即可。
