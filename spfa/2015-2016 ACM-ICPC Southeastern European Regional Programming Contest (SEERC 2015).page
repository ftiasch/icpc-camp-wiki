[Contest Link](http://codeforces.com/gym/100818)

A. Equivalence \[Nero\]
=======================

题意：给两个最多51个变量的真值表达式，包含括号，与，或。问他们是否等价，即对于所有的变量赋值，输出都一样。

题解：给变量随机赋值然后判断表达式值。时限0.2秒，所以随机到0.18秒就可以停了。

B. Tree of Almost Clean Money \[\*Nero\]
========================================

题意：给一棵n(&lt;= 5e5)节点的树，有初始点权。支持m(&lt;=
5e4)次操作：把给定参数生成的K(&lt;=
1000)个点各加上一个值，然后询问(u,v)路径权值和。

题解：首先树转序列。然后
需要一个支持O(1)修改，O(不超过K)查询的数据结构。那么就分块吧。此处分块修改复杂度恰好是O(1)
orz。

C. Primes \[\*JayYe\]
=====================

题意： 给你一个数n(n &lt;=
1e10)，每次可以对这个数随机变成两个数x,y满足x&gt;1 && y&gt;1 &&
x\*y=n，剩余的数也是这样变化，直到所有数都不能变，问期望次数。

题解：
对于n，我们关心的只是素因子的个数和种类，所以可以预处理出所有情况的素因子个数，然后设dp\[i\]\[j\]表示第i种情况变化次数
&lt;= j的概率，然后跑的很慢，需要打表。

D. LCM \[JayYe\]
================

题意： 给你两个自然数A,B (A,B &lt;=
1e9)，求最小的自然数N满足A+N和B+N的最小公倍数最小。

题解： 令A &lt;= B，设d = gcd(A+N, B+N)，则A+N = xd, B+N =
yd，两个式子相减得到B-A =
(y-x)d，所以d是B-A的因子，枚举下搞搞就好了。只是毛子的三观和我们不同，自然数不包括0！坑了我4小时，严重影响心情导致这场崩了。

E. Taxies \[JayYe\]
===================

题意： 有K(K &lt;= 15)在等车回家，家在n(n &lt;=
2e4)个城市，你需要租若干辆车把K个人送回家，每辆车有花费，城市间的路也有花费，问最少花费。

题解： 对K个点跑最短路后状态压缩dp搞下就好了。

F. Irrational Roots \[Nero\]
============================

题意：给一个n(&lt;=
8)阶方程，最高项系数为1，常数项不为0，保证所有的n个根都为实数，且不小于-10，不大于10。问有几个根是无理数。重根算多次。

题解：首先猜测 所有的非整数根都是无理数。然后 方程能够化成 (x - x1) \*
(x - x2) \* ... \* (x - xn) = 0 这样的形式。那么把整数根项(x -
xi)消去，剩下的都是无理数根了。

G. Race \[Nero\]
================

题意：有一张n(&lt;=
1000)点的无向图，把点们分为两个集合，使得对于每个点，它相邻点中与它同集合的点数不超过最大点度的一半。

题解：先把全部点扔到一个集合里。然后每次找到相同值最大的一个点，把它丢到另一个集合里。重复过程直到满足要求。

H. Railway Tickets \[Nero\]
===========================

题意：一列火车有K(&lt;= 1000)个座位。从1开到n(&lt;=
1e5)。已经卖出了K(&lt;= 1e5)张票(p,s,e):座位在p，起点s，中点e(s &lt;
e)。问有多少对(起点，终点)对，可以通过剩下的票转车(使用大于1张票)到达。

题解：处理出每个座位的空闲时段，搞出站点i作为起点坐一张票最远到哪里，以及最远可以转到哪里。

I. Olympic Parade \[Nero\]
==========================

题意：给n(&lt;= 1e5个数(int)，找出其中唯一一个出现次数不足m(&lt;=
1e5)的数。空间为4M。

题解：分部分统计然后合并一下。

J.
==
