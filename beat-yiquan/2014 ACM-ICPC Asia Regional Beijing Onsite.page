[Front Page]()

A.
==

签到题

B.
==

贪心+构造。 无解条件：考虑图是一个二分图，分黑点和白点（WLOG,
假设黑点个数B = ceil(n*m/2), 白点个数 =
floor(n*m/2))。如果有一种颜色的出现次数超过B，那么显然无解，否则有解。
构造方法：把颜色按照出现次数排降序，遍历一遍，能往黑点塞往黑点塞（从上向下），不能的话尝试着往白点塞（从下向上），如果还是塞不了，取出当前颜色C留作特判，剩下的继续塞（可以证明剩下的一定可以塞下）。然后把C塞到上下的点上就好了，可以证明C所在的黑点和白点不会相遇。

C.
==

求出球的坐标对t的公式，解同余方程，用中国剩余定理。\
每个坐标的可行解的暴力求。

D.
==

只要考虑b数组，区间DP，dp\[i\]\[j\]表示删掉i,j之间的（不含i,j）最小花费\
dp\[i\]\[j\] = b\[i\]\[j\] + min{dp\[i\]\[k\]+dp\[k\]\[j\]}

E.
==

前缀和思想。求相交的L的数量，分两种情况：其中一个L的角被另一个覆盖；任何一个L的角都不被覆盖\
用cnt\[x\]\[y\]表示1&lt;=i&lt;=x,1&lt;=j&lt;=y,gcd(i,j)=1的(i,j)数量

F.
==

高斯消元。01计数的时候可以利用x^2^=x。\
考虑展开。

G.
==

暴力存下前1e8个排名的答案。手动模拟dfs也许会稳一些。

H.
==

开始用了中间相遇，其实可以暴力DP。

I.
==

没有圆面积交模版。少考虑的情况：圆内含；优弧劣弧；半径为零（分类边界）

J.
==

K.
==

签到题。 对于有一个a\[i\], 如果存在a\[j\] &lt; a\[i\] 且 j &gt; i,
那么一定要挪一下a\[i\]。答案是满足前面条件的a\[i\]的个数。
证明：从最大的数往最小数进行操作。
