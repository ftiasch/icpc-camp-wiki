今天准备和cls打一下开火车上的Northern Subregional
2015，但是cls带dls去东操跑步了，一直没有出现。

开场先过了sb题abce，e题sb了一下wa了一发。然后过了sb题lh，h随机搞搞就行了。

d也不是太难，每次把2和3的因子除完，然后找最大的3的次幂减掉，就好了。

j是个sb题，但是一开始看错题意坑了很久。（wa了一发，漏一处long long）

f是个模拟他执行然后插值。（也wa了一发，忘记长度《=100）

K按斜率排序转一圈，n\^2 log n,
写完发现wa了。拍了很久没发现错。后来发现题目是distance to
segment而不是to line。 最后改了一下，然而TLE了 GG。

dls最后写了一下I的乱搞，居然过了。后来发现这个做法其实很对。

总结：

读题要仔细
