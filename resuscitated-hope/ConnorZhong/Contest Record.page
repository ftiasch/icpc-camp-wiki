Leetcode Weekly Contest 41 (4/4) Done
-------------------------------------

-   `Leetcode 636` 输入比较恶心。拿个栈记录模拟下调用。
-   `Leetcode 642` 发现最近 Leetcode
    出了一些这种类似于工程项目的题，感觉挺无趣的，没什么脑子 xjb 调 map
    和 string 的库。
-   `Leetcode 643` 第一眼看上去感觉好难啊，然后发现 $k$
    tmd竟然是固定的？？
-   `Leetcode 644` 考虑二分答案。相当于给定平均值 $a$
    去查有没有这样一段。然后考虑序列
    $a[i]-a$，求和最大的部分是否大于等于 $0$。于是拿个双指针，每次
    $j - i \geq k$ 时，一旦 $i$ 到头部的和小于
    $0$，就把头砍去，同时维护遇到的区间最值。

Codeforces Round \#418 （4/5）
------------------------------

-   `CF 814A` 构造不增序列，由于数字 unique 所以$k > 1$ 时必然可行。
-   `CF 814B`
    由两序列和目标序列都恰好一处不同，因此至多有两个地方不同，枚举一下放的方法，需要注意的是给出的两个序列不一定是排列。
-   `CF 814C` 单次询问可以利用尺取做到 $O(n)$，这道题显然有一个
    $O(n^2\Sigma)$ 的暴力，枚举所有区间就可以。
-   `CF 814D` 定义圆的嵌套层数为 $k$，易发现 $k$ 为 $1$ 和
    偶数时计算正值，否则计算负值。有一个类似的题 `bzoj 4561`。

Educational Codeforces Round \#22（5/6）
----------------------------------------

-   `CF 813A` 求个和。
-   `CF 813B` 暴力枚一下所有的情况，排个序。
-   `CF 813C` 最优情况是，Bob 先往根跑，Alice 往 Bob
    那跑，即将相遇时，Bob 往深的地方跑。
-   `CF 813D` 令 $f(i,j)$ 表示两个序列分别以第 $i,j$
    个数结尾时的最大长度和。转移
    $f(i,j)=f(j,i) (i < j), f(i,i) = 0, f(i,j) = \max(f(k,j))+1(i>j)$，$k$
    为可转移的值。因此维护一个遇到过的 $a[k]$ 的最大值和模 $7$
    的最大值。
-   `CF 813E` 考虑查询 $l,r$，令 $f(i)$ 表示自己前面第 $k$
    个同名元素的位置，无则赋值为 $0$。等价查询 $i$ 位于 $l,r$ 之间且
    $f(i) < l$ 的数目，于是树状数组或者线段树维护一下各个 $f(i)$ 的
    $i$。当然也可以可持久化离线版本：离线按 $r$ 排序，维护 $1,r$
    的查询。

Codeforces Round \#417（5/5）**Done**
-------------------------------------

-   `CF 812A` 傻逼讨论？
-   `CF 812B` 暴力dfs就可以了。
-   `CF 812C` 二分后sort啊。
-   `CF 812D` 题意杀？request？然后就是树上查子树傻逼题了
-   `CF 812E` 奇数层偶数层分开，叶子结点所在的奇偶数层做 Nim，证明也和
    Nim 类似，计数大概就随便做了。

SnackDown Online Pre-elimination round A 2017（6/6）**Done**
------------------------------------------------------------

-   `CodeChef TEAMFORM` 水。
-   `CodeChef ISSNAKE`
    枚举最靠左的头部，如果在同一列有两个头部需要分别枚举，接着就走一下轨迹就可以了。
-   `CodeChef SNTEMPLE` 可以$O(n)$
    递推算出每个点的一侧能够生成的斜坡的最大高度，左边右边分别做一遍，取一下$min$
    的最大值就是最大高度。
-   `CodeChef CONSESNK`
    二分队首的位置。贪心将后面的人站队。可以计算出每次排队时向前和向后走的人数。当向前向后走的人数达到平衡的时候则为最优解，正确性易证。
-   `CodeChef PROTEPOI` 横着竖着分别贪心走就行了，由于蛇不存在于
    $K\times K$
    的区域中，因此互不干扰。没有发现条件都是奇数的意义。如果去掉不存在于
    $K \times K$ 区域这个条件该怎么做呢？
-   `CodeChef GAMEBALL`
    构造。看丢了只有一个空，其他都是球的条件，于是写的很复杂，变成了加强版（？），其实不存在的。只有在$n,m$
    同时小于 $3$
    且当且情况不合法才无解。其他时候就把空格移到第一行末尾，一行一行消除，然后剩一列，再来一遍。

SnackDown Online Qualifier 2017 (4/4) **Done**
----------------------------------------------

-   `CodeChef SNAKPROC` 水。
-   `CodeChef TEMPLELA` 水。
-   `CodeChef SAMESNAK` 分类讨论。
-   `CodeChef SNAKEEAT` 对于每次询问二分答案。为了能够 $O(1)$
    Check，需要排序二分位置，维护一个前缀和。

Leetcode Weekly Contest 37 （4/4）**Done**
------------------------------------------

-   `Leetcode 621`
    容易想到时间长度至少与最多的任务所需的时间相匹配，其他的任务总是可以通过调整间距塞进空隙。所以只要求出最多的任务，和并列最多的任务种类数就可以了。
-   `Leetcode 623` 水。
-   `Leetcode 624` 把最大的两个数找出来，最小的两个数找出来。
-   `Leetcode 625` 分解质因数，因为每个位不能超过
    $9$，把质因数从大到小贪心组合一下，注意细节。

