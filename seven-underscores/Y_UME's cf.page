4.12

`303C` push\_back好慢

`762D` 分析一下转移

`329C` 合并一些环，注意极限情况

4.5

`45C` 水

`45B` 并查集

4.4

`360C` 复杂度n\^2lgn

`755F` 容量限制背包

`354D` dp状态

`354B` 水dp

4.3

`690C3` 经典题

`771D` 逆序对dp

`771E` 状态减少的dp优化，记忆化作法复杂度待补

3.31

`39A` 贪心

`18E` 四向DP

`744C` 反向思考的DP

3.24

`509F` 水dp

`212E` 水

`650D` LIS套路题

`486C` 树状数组找第K大get

3.23

`632E` 可以FFT,可以转成最短路问题

3.22

`31E` 水

3.19

`715C` 点分治

`464E` 主席树好题目

`547D` 二分图构造

3.18

`607C` 结论题 + KMP

`382D` 水题，注意空的vector空间挺大的

`575G` 广搜，实现技巧

`403D` 简单组合公式,有一个精妙的nsqrt(n)的做法

`348D` Lindström–Gessel–Viennot lemma

3.17

`498D` lcm线段树

`435D` 预处理前缀和+暴力枚举

`328D` 水题

`253D` 滑窗

`28A` 水题

`417E` 水构造
