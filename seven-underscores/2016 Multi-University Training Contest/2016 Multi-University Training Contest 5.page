**A ATM Mechine**

报错次数不会到达$lgn$次。$W=min(W,15)$。暴力转移。
现场打表发现最优转移点在几次后不变。

**B Cycle**

用KMP和exKMP处理出匹配的情况，biteset优化判定。

**C Divide the Sequence**

栈贪心模拟一下。

**D How many Triangles**

用任选三点方案减去钝角在减去三点共线的方案，求钝角的方案可以考虑极角排序，然后两个指针扫一圈就可以找出90度到180度之间的点，atan2精度要开到1e-12，可以用整数极角排序以及判象限的方法避免精度误差

**E Interesting**

回文树。卡空间过的。manacher会好点。

**F Interval**

**G K-wolf Number**

数位DP。暴力记录前K个数字转移。

**H Level Up**

找区间第k大可以用线段树合并或者启发式合并，然后知道每个结点中位数和中位数右边的数字，就可以知道修改不大于中位数的值，可以让中位数变成他下一个数字，因此进行一个dfs，把值离散化，过程用树状数组记录每个值可以产生的差值，到叶子结点的时候更新一下答案即可

**I Permutation**

**J Prefix**

考虑把字典树上的每个结点经过的时间结点抠出来，等价于求区间内本质不同的结点个数，并且要支持强制在线，所以可以用主席树，固定每个右端点r，每次到一个位置，把这个位置的结点到前一个结点的区间值+1，询问就在相应的r里面查找l的答案

**K Two**

$N^2$ DP。现场树状数组$N^2lgN$

**L World is Exploding**

树状数组维护处每个数字前面后面比它大和小的数字个数，那么就可以用一个可以重复的总方案数，减去$(A, C), (B, D), (B, C), (A, D)$选到同一点的方案数即可
