**A A Boring Question**

打表看出答案是$m^n - m - 1$

**B A Simple Chess**

马只有两种跳法，可以列两个方程，解方程把点转换到正常的平面坐标系上，就变成一个经典的容斥，$dp_i$表示到i的方案，等于$C_{n + m}^n - dp_j * C_{x + y}^x$，然后由于模数小，需要lucas定理

**C A Simple Nim**

打表看出8的倍数x的sg函数和x-1逆序。

**D Magic Number**

**E Master Zhu**

**F Stabilization**

对于相邻两个数字$a$和$b$，设定$x$后值为$a-b$还是$b-a$取决于$a$和$b$表示成二进制数后，从高位开始数第一个二进制位不同的位置，因此可以开一个数组$f(a,b,c,d)$，表示控制位是$a$的情况且第$a$位为$c$，数值统计位为$b$且第$b$位为$d$的权值和，可以用$O(400N )$的复杂度求出该数组，然后使用记忆化搜索搜出$x$为1到2\^20的所有答案。复杂度$O(20*2^{20})$

**G This world need more Zhu**

树上莫队的模板题

**H To My Girlfriend**

dp,用$f(n,m,c,d)$表示处理了第1个到第$n$个数字，并且选取了和是$m$的情况，选取了$c$个必然选取的数字，$d$个必然不选取的数字的方案数，dp即可，复杂度$O(4nm)$。

**I Up Sky,Mr.Zhu**

由于回文串长度很短，且字符种类数少，对于答案可以分别统计每种长度的回文串种类，每种长度回文串开个可持久化字典树统计，假设当前回文串长度为i，询问区间$(L,R)$，那么如果回文串长度为奇数，则变为询问$(L+i-1,R-i+1)$,否则变为询问$(L+i,R-i+1)$，由于串长最多为10，且有奇数偶数之分，所以时间复杂度为$O(200n)$

**J Windows 10**

贪心，能降则降，降至不能下降了，采取两种策略，再降一次后上升，或者停顿，上升可代替停顿，注意音量不能将至0以下。

**K Zhu’s Math Problem**

数位DP，把数字换成二进制，由于有4个数字，所以dp状态需要记录到$[-3, 3]$，超过3之后大小关系就不会在变化了，然后记忆化搜索dp即可。
