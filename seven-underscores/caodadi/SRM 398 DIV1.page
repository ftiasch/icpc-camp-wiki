250：暴力枚举即可

500：$dp_{i, j, k, l}$表示在位置$(i, j)$，最后一次访问的为k，访问个数为l的方案数，转移就往右下两个方向转移即可

1000：所有点的总度数为$sum / (n - 2)$，那么对于每个置换循环可以列出方程$x1 + x2 = s1, ..., xn + x1 == sn$，那么由于个数是奇数个，所以方程可以相邻两项相消，求出$x1$，进而求出每项的值，即每个点的度数，得到每个点的度数就转换为一个经典问题，给定每个点的度数判断能否构成一个无向图，贪心即可
