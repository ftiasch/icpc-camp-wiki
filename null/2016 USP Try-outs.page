A. Renzo and the lost artifact\[hx\]
====================================

原题，一个做法是先随机选一个点，找出在两个矩形中的位置，如果不重合，取两点连线的中点递推下去，直到重合。

正解是得到转换公式，然后列方程求解。

B. Buffaloes\[hx\]
==================

不正经的做法：OEIS。
正确的做法应该是：dp\[i\]\[s\]表示前i个数，单调栈的状态为s（先只管大小关系，不管具体数），每次枚举下一个数的大小关系，然后转移。

C. Cahokia ruins\[lyj\]
=======================

枚举每行判断

D. Black Hills golden jewels\[lyj\]
===================================

二分

E. A Word to Trump All\[hx\]
============================

先对所有串建立AC自动机，然后f\[s\]\[i\]表示s集合中的horrible
word(s)已经出现过，当前在AC自动机的i号点是否可行，然后枚举下一个字符转移。

F. Metal detector\[hx\]
=======================

相当于每次删去所有奇数/偶数的点，然后剩下的重标号，模拟即可。

G. The Declaration of Independence\[lyj\]
=========================================

把版本建成一颗树,然后dfs,用个双端队列维护即可

H. Pop Divas\[hx\]
==================

分别枚举选两个集合中的数，用map存储查询。

I. Protecting the Central Park\[lyj\]
=====================================

树形dp

J. King of Tokyo
================

K. Mount Rushmore and Birthdays\[hx\]
=====================================

枚举答案即可

L. The Knapsack problem\[lyj\]
==============================

乱搞,找出密度最大的物品,然后大部分的体积全放这个,剩余的体积dp
