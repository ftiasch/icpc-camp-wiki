总共13题,完成7题.

A - Too Rich\[hx\]
==================

考虑到相邻两种钱币至少是两倍的差距,且只有20,50以及200,500之间不是刚好2倍,而是2.5倍.<br />
如果全都是整数倍关系的话,实际上可以直接从大的币值到小的贪心的选,使当前币值使用的张数尽量少.而由于有2.5倍关系的存在,那么当前币值可能需要多取一张,分两种情况DFS下去就行.复杂度$O(T2^10)$

B - Count a \* b(补)
====================

贴糖老师[blog](http://blog.csdn.net/skywalkert/article/details/49276505)

C - Play a game(补)
===================

设$f[l][i]$表示从$i$开始的长为$l$的子串是否先手必胜,如果这一段不属于给定的字符串集,$f[l][i]=!f[l-1][i] | !f[l-1][i+1]$,用bitset优化.而对于给定的字符串,由于长度之和不超过10000,分两种情况讨论:如果长度不超过100,加入trie,最后一起判;如果超过100,这种串少于100个,直接暴力hash判.

D - Pipes selection
===================

E - Rebuild\[lyj\]
==================

选取第一个点设半径为r，可以递推出其它的圆的半径，最后得到一个关于r的一元二次式子求最小值即可

F - Almost Sorted Array\[syx\]
==============================

纱布题

G - Dancing Stars on Me\[lyj\]
==============================

求个凸包，先看凸包上是否有n个点，再看每条边的长度是否相等，再判断每相邻两条边的叉积是否相等即可

H - Partial Tree\[hx\]
======================

实际上相当于给n个点分配度数,每个点度数&gt;=1,使得总数之和为2n-2,那么做个背包就行了.

I - Chess Puzzle(补)
====================

视横坐标相差a,纵坐标相差b的点对为相邻点对,给他们连边,可以构成一个二分图,考虑答案即所有相邻的点对减去颜色相同的相邻点对,然后就是一个经典的最小割问题.<br \>
网络流建图:对原图的一条边&lt;u,v&gt;,建立从u到v流量为1的边,从v到u流量为1的边.从源点向X部黑点以及Y部白点连流量为oo的边,从X部白点以及Y部黑点向汇点连流量为oo的边.<br \>
求字典序最小可以枚举每个点,如果给它定为黑色点不会使答案变化就为黑色,否则为白色(除非姿势优美才能过)<br>
红太阳糖有更优美的做法：先从s走没有满流的正向边，能到达的点都标记为0，因为这些点都一定属于s集合，所以从s向它们连边仍然不改变这个性质，那么连边以后在X部的点都是黑点，在Y部的点都是白点(实际上可以不连这些边)。同理再从t走没有满流的反向边，到达的点都一定属于t集合，把这些点标记为1
然后一次每行每列考虑每个点，若它没有标记，就可以让它成为黑点，那么如果它在X部就假设它和s连边，否则和t连边，然后用0或者1更新其它的点即可

J - Chip Factory\[hx\]
======================

先将所有数插入到trie中,然后枚举i和j,将$s_i$和$s_j$从字典树中先删除,然后查询最大值,再重新插入.

K - Maximum Spanning Forest(补)
===============================

离散化之后就是个$200\times 200$的图,如果每次暴力kruskal的话,由于要排序,是$O(n^3\log n^2)$的,无法通过,注意到一个事实,只有上次kruskal保留的边才有可能在这一次也保留,因此可以按权值大小枚举上次使用的边,在适当的时候插入当前值为c的边,省去了排序的复杂度.

L - House Building\[lyj\]
=========================

枚举每个正方形的五个面判断有没有别的正方形挨着这个面即可

M - Security Corporation(补)
============================

首先可以找到所有交点,由于没有三线共点,每个交点最多和四个点相邻,那么我们将所有点按坐标排序,然后贪心的染色,由于每个点的相邻的点中最多只有两个已经染了色,所以只需要三种颜色.<br />
对于只需要一种颜色的情况,即没有点相邻的情况以及只需要两种颜色的情况即相邻关系是个二分图的情况特殊判断一下.
