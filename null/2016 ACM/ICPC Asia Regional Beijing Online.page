A The Book List\[lyj\]
======================

像trie那样建树并且每个节点开一个set

B Genius ACM(补)
================

暴力判断一个区间$(l,r)$的复杂度为$O((r-l+1)\log (r-l+1))$。考虑每次贪心，即固定左端点，找最大的可行的右端点。首先找到最大的k满足$(l,l+2^k-1)$这段区间可行，那么要找的右端点必然在$[l+2^k-1,l+2^{k+1}-1)$之间，二分一下即可，不难证明复杂度为$O(n\log^2 n)$

C A Simple Job\[lyj\]
=====================

字符串处理

D Pick Your Players(补)
=======================

水DP。f\[i\]\[j\]\[k\]\[l\]\[p\]\[q\]表示前i个人用了j点cost买了k个D，l个M，p个F，q个G的最大价值，由于队长双倍价值，把所有人按照价值排序，第一个选的人就是队长。实际上数组的第一维可以不要，方案数在DP的时候搞一下就行。

E A Research on "The Hundred Family Surnames"\[hx\]
===================================================

我们的做法是离线做，树分治。考虑第一次分治的时候，更新每一个询问，然后对于一个询问，最多只需要下放到一个子树里面（即两种颜色到根的距离都是最大的那个子树），最多下放log次。<br \>
实际上，答案只需要考虑两种颜色分别的直径，最多4个点（参考51nod的某题），维护下lca即可，这样很好写。

F Periodic Signal\[hx\]
=======================

把b翻转并且把a，b都扩充1倍之后，做一下FFT就能得到对于每个k的答案，由于精度要求较高，需要把$a_i$和$b_i$拆成$x_i\times 2^{10}+y_i$的形式多做几次FFT。

G Sewage Treatment
==================

H Ada's Needle Experiment
=========================

I Countries\[hx\]
=================

对于每个导弹求出抵挡它防护罩需要覆盖的范围，然后按右端点排序扫一遍用堆维护一下即可。

J War Chess
===========
