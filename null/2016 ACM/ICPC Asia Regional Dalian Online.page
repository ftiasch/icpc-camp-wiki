[一份题解](https://async.icpc-camp.org/d/546-2016)

1001 Different Circle Permutation\[hx\]
=======================================

lisa在oeis上找到数列然后得到了公式：$\frac{1}{n}\sum_{d|n}\varphi(\frac{n}{d})(fib_{d+1}+fib_{d-1})$，
然后对n=1特殊处理一下。

正解的话，先考虑没有旋转的方案数，为$f(n)=f(n-1)+f(n-3)$，加上旋转的话，
套用burnside引理，有$F(n)=\frac{1}{n}\sum_{i=1}^{n}f(gcd(i,n))=\frac{1}{n}\sum_{d|n}f(d)\varphi(\frac{n}{d})$

1002 Different GCD Subarray Query\[hx\]
=======================================

对询问按左端点从大到小排序，利用固定左端点，字串的gcd只有$O(\log)$种的性质，得到这$\log$个gcd的值和需要的最小的右端点，设$b[i]$为i作为区间gcd所需的最小的右端点，那么询问相当于$b[i]\le R$的i有多少个，利用树状数组维护即可。

1003 Alice's Adventure in Wonderland
====================================

1004 Number of Connected Subgraph
=================================

1005 Seats
==========

1006 Football Games\[hx\]
=========================

首先，每个人得分的总和必须是$n(n-1)$，然后任取一个子集，他们的得分必须在一个范围内，根据一个神奇的定理，只需要对得分从小到大排序，对每个前缀和，满足$pre[i]\ge i(i-1)$即可

1007 Friends and Enemies\[hx\]
==============================

感性的想，不断加一个点，我们要让它不能选之前出现过的所有值，并且要让和它是朋友的那些人相互之间都是敌人，那么分成二分图即可，两个部分内部互为敌人，相互之间互为朋友，并且保持两部分人数之差的绝对值最小。

1008 Function\[lyj\]
====================

每个点为左端点记录以它开始变化的位置和数，最多一共只有nlogn个,lower\_bound查询个数

1009 Sparse Graph\[lyj\]
========================

点数小于6000暴力跑,否则S连接的点为1,其余为2

1010 Weak Pair\[hx\]
====================

搞出DFS序，然后对每个点询问子树内值在一个区间内的个数，用主席树即可。
