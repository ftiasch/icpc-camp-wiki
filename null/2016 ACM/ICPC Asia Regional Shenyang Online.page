1001 Resident Evil(补)
======================

做法同[cf341D](http://codeforces.com/problemset/problem/341/D)<br \>
考虑一维的情况，维护差分序列，这样修改就只需要修改两个值，如果是单点查询，则为求前缀和，如果是查询前缀和，则是求前缀和的前缀和。考虑如果查询的前缀和是奇数个，那么根据异或操作的性质，只有差分序列中奇数位置的值会被异或起来，如果是偶数个，只有偶数位置的值会被异或起来，用树状数组分别维护差分序列奇偶的前缀和即可。二维同理。

1002 List wants to travel(补)
=============================

由于题目给的是边权,把边权放到点上 轻重链剖分后
线段树每个点维护该点所代表区间中有多少个连续的颜色相同的段
只需要每个点维护一下左端点的颜色和右端点的颜色就行了
然后修改就直接打标记 查询时要特判lca是端点的情况

1003 hannnnah\_j’s Biological Test\[hx\]
========================================

枚举第一个放哪，分大于k和小于等于k两种情况讨论算一下

1004 Mathematician QSC\[hx\]
============================

利用$a^b\equiv a^{b\;mod\;\varphi(p)+\varphi(p)}(mod\;p)$，求出$g(n\times y)$在模$\varphi(n*y)$下的值，然后就行了。

1005 Running King\[hx\]
=======================

答案即总的图的个数减去森林的个数，n个点的森林的个数递推公式为$f_n=\sum_{i=1}^{n}\binom{n-1}{i-1}\times i^{i-2}\times f_{n-i}$。把组合数拆一下，分治NTT加速。

1006 The Game
=============

1007 odd-even number\[hx\]
==========================

数位DP

1008 oasis in desert(补)
========================

先跑floyd，建新图：如果两个点之间距离小于等于k则连边。根据题意，即求一个满足既是最大独立集又是最小点覆盖的集合，对于一个图，最大独立集+最小点覆盖等于n，所以这个集合的点数必定是$\frac{n}{2}$，且在这个集合中的点互相没有连边，否则不满足最大独立集，不在这个集合中的点互相也没有连边，否则不满足最小点覆盖，所以这个图是个二分图，二分图的最小覆盖大小等于最大匹配数，于是该图必定能够完备匹配，于是只需要$O(n^3)$做匈牙利即可。

1009 QSC and Master\[lyj\]
==========================

水dp

1010 Count primes\[hx\]
=======================

板子题，复杂度$O(n^\frac{2}{3})$
