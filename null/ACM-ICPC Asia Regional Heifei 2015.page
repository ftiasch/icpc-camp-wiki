共10题,完成2题

A - Bus Routes(补)
==================

即连通图的个数减去树的个数<br \> 树的个数即$n^{n-2}\times m^{n-1}$<br \>
设$f[i]$表示$i$个点的连通图的个数$f[i]=\sum_{j=1}^{i-1}f[j]\times (m+1)^{C_{i-j}^{2}}\times C_{i-1}^{j-1}=(i-1)!\times \sum_{j=1}^{i-1}g[j]\times h[i-j]$<br \>
直接暴力算是会TLE的,需要分治NTT优化,总复杂度$O(Tn\log^2 n)$

B - Advanced Calculator
=======================

C - Autopilot System
====================

D - Immortality of Frog(补)
===========================

浪哥教我状压dp
其实就是求方案数，对于一列上有超过10个坏膜的情况显然答案是0，否则每一列上最多10个坏膜，剩下的膜都是好膜(覆盖了1到n列的膜)<br>
设f\[i\]\[j\]表示前i列，j表示覆盖第i列的坏膜有哪些被i之前的青蛙得到了药(用1表示,否则0表示)<br>
然后枚举第i只青蛙是吃了某个好膜上的药还是某个没被吃过的坏膜上的药，更新f\[i+1\]\[z\]<br>
利用j可以推出i+1的情况j'，就不需要每次都话O(10)的时间重新算出z了，也就是可以O(1)从j'得到z了<br>
复杂度O(10n2\^10)

E - Land of Farms\[hx\]
=======================

只有最多10个古农场,可以$2^10$枚举选哪些,然后对剩下的还能选的区块,做个最大匹配就能得到答案.

F - Matching Compressed String(补)
==================================

f\[i\]表示i号点经过一段转移后到达的点，如果没有这个点则设为n。那么对于**R(S1)**这种，用快速幂可以快速算出。

G - Alice's Classified Message(补)
==================================

后缀自动机直接模拟

H - Frog and String\[lyj\]
==========================

经过实践以及找规律以及浪哥的神奇构造发现m大于n时无解；m=n时全部输出A；m小于n时：k=1无解；k=2:n小于8时无解；n=8时m=7时输出AABABBAA(除此之外还有另外三组解)；n大于8时:m小于8无解；m大于等于8先放m-8个A,不足n位就按照AABABB中的每个字符依次放；k大于2:m小于3无解；m大于等于3就前n-m+3位按照ABC中的每个字符依次放,最后m-3位全部放第n-m+3上放的字符

I - The Shields(补)
===================

又是一道离线的题目,这个可以用离线+树状数组搞<br> ![alt text](/1.png)<br>
在三角形ABC中的点的数目=在AC,AB之间的点的数目-在CC1,CB3之间的点的数目+BB1,BB3之间的点的数目-BB1,BB2之间的点的数目<br>
实际上在CB上的点也是要算的,所以要把C和B都向下移动一点点,一种可行的方法是把所有点的横纵坐标都\*2,然后C点+=(-1,-1),B点+=(0,-1)<br>
要开两颗树状数组,然后把事件点按y-x排序,导弹也按y-x排序,然后枚举事件点,用个指针l表示当前考虑第l个导弹<br>
导弹的y-x&lt;=事件点的y-x,就把导弹的x+y丢到一颗数组,x丢到另一颗数组,查询就在对应的树状数组里查询就行了

J - Kingdom of Tree
===================
