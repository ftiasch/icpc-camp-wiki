A.Best Matched Pair\[zsc\]
==========================

暴力枚举check就可以了

B.Help the Princess\[zsc\]
==========================

可以先bfs算出来到每个点哨兵的最短路
然后如果公主走到那里时间&lt;哨兵就能走 否则不能走过去

C.We don't wanna work\[zsc\]
============================

按照题意模拟用个set即可 (set原来可以直接删除值为x的东西2333

D.Parentheses\[zsc\]
====================

考虑长度为2x的 最坏情况即为)))...)(((...(

这样f\[x\]=f\[x-1\]+x

于是可以算出来长度和第一个(位置 然后输出即可

E.Similarity of Subtrees\[zsc\]
===============================

直接hash

F.Escape from the Hell\[zsc\]
=============================

我们考虑贪心 考虑第i天 肯定找能爬最高高度的能不能爬出去 如果能就结束了

如果不能的话 应该找一个上-下最大的爬 这样是最优的

但是这时候有个问题 就是有可能上-下很大的上也很大

比如两组 100 50和 2 1

整体高度是101 那么应该是先2-1 再100就出去了

但是这样贪心会先走100-50

于是我们还要存下来已经走过的方案

但是换到后面还要保证前面不被抓到
于是发现可以被换到后面的肯定是走的里面的最后几个 而且单调

于是实时维护能换到后面的里面的上最大的就可以了

G.Share the Ruins Preservation\[zsc\]
=====================================

可以求出前i个点的凸包面积和后i个点的凸包面积

我先求前i个的上凸壳 然后翻y坐标就是求i个点的下凸壳
然后翻x就是后i个的了最后拼起来就可以了

H.Pipe Fitter and the Fierce Dogs
=================================

I.Multisect
===========

J.Compressed Formula
====================

K.Non-redundant Drive
=====================
