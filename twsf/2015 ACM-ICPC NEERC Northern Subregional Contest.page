[题目链接](http://codeforces.com/gym/100801)

A. Alex Origami Squares \[zxa\]
===============================

水题

B. Black and White \[zxa\]
==========================

分4层上面两层全黑下面两层全白，然后少几个就在第一层最后一层补几个即可。

C. Concatenation \[jsy\]
========================

为了保证不重复，在相同的方案中要使得第一个串的前缀尽量长。对于一个前缀$S[1..i]$，后面不能接以$S[i+1]$为开头的后缀。两个串都是非空，要注意一下这种情况。

D. Distribution in Metagonia \[jsy\]
====================================

构造：每次提取尽量多的$2$的因子，使得$n$变成奇数，减掉不超过$n$的最大的$3^k$。不断重复这个操作，直到$n$变为$0$为止。

E. Easy Arithmetic \[jsy\]
==========================

在每个减号后面的数字贪心填加号。

F. Fygon \[zxa\] \[jsy\]
========================

将n等于0到6代入，求出代码执行的次数，然后把系数插值出来。

G. Graph \[jsy\]
================

拓扑排序，用$set$保存所有入度为$0$的点。如果$K$为$0$，每次弹出标号最小的点即可。如果$K$不为$0$，就可以让某个还没处理的点向标号最小的点连边使得字典序变大，而这时候我们不知道让哪个点向它连边，所以我们再开一个$set$维护这些被连边的点。这里需要分几种小情况讨论讨论。

H. Hash Code Hacker \[zxa\]
===========================

乱搞出hash值相同的单元，就很容易构造了。

I. Insider's Information \[jsy\]
================================

考虑从中间开始往两边生成序列。对于每个限制$(a,b,c)$，应该满足先生成$a,b$再生成$c$，或者先生成$b,c$再生成$a$，由于一定可以构造出一组满足所有限制的合法解，所以肯定能实现这样的生成顺序。当填入数字$x$时，假设它是限制集合$S$最后一个填入的数字，那么他填序列的左边满足限制$S1$，填序列的右边满足限制$S2$,且$S1$交$S2$等于$S$。所以我们每次填一个方向使得满足的限制条件尽量多，这样就能保证最后满足的限制个数不少于一半。

J. Journey to the "The World's Start" \[zxa\]\[jsy\]
====================================================

把好票都挑出来，好票的代价应该随距离的增加而增加，然后二分，用DP来check。

K. Kingdom Trip
===============

L. Lucky Chances \[zhy\]
========================

按题意模拟
