A. Where is the Boundary \[jsy\]
================================

水题，统计一下$W$和$E$的前缀和即可。

B. Vector Field \[zhy\]
=======================

枚举从哪个点开始加速，离散化后在$set$中查询是否能找到下一个加速点，找到答案加1并将这个点从$set$中删除。最后更新答案即可。

C. Kuru Kuru Sushi
==================

D. Identity Function \[zxa?\]
=============================

题目就是要求个最小的$k$使得$N^k \equiv 1 \mod \varphi_N$，如果$\gcd(N , \varphi_N) \not = 1$肯定无解，所以$N$必须要是一个无平方因子的数，对每一个质因子分别考虑，答案必然是质因子的$\varphi$的最小公倍数的约数，一个一个check即可。

E. Enclose Points
=================

F. Marching Course \[jsy\]
==========================

答案一定是花费一些时间走到一个点，然后在那个点周围找个性价比最高的一条边一直走，最后再原路回去。

G. Surface Area of Cubes \[jsy\]
================================

用$map$维护每对$(x,y)$中$z$被分成的段数，它对答案的贡献是段数的两倍，其他两个方向同理。

H. Laser Cutter \[jsy\]
=======================

把线段的起点都放在左图，终点都放在右图，左图所有点到右图都连一条边，权值为两点之间的长度，跑一遍$KM$即可。

I. Live Programming \[jsy\]
===========================

唱歌一定是按照$f$值单调递增或者递减的顺序唱的。

设$F[i][j]$为花了$j$分钟处理了前$i$首歌且第$i$首歌最后唱所得的最大代价。

$F[i][j] = max(F[i - 1][j], F[k][j-ti] + pi +(fi - fk)^2)$

这个方程化简后就可以斜率优化啦。

J. Black Company \[jsy\]
========================

对于每个点$x$，把$x$和它相邻的所有点都抠出来按照$ci$排个序，从前往后依次按照偏序关系连边(相同的合并成一个点)。对于所有点大于$0$这个限制也要连边，这样得到的一定是一张拓扑图。在拓扑图上做一遍最长路即可。
