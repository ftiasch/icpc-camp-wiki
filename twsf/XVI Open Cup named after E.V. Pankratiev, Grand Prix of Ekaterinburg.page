A. Avengers, The \[zxa\]
========================

将点集随机三角剖分，构成一棵树，叶子都是空的三角形。

询问直接递归。

B. Black Widow \[zhy\]
======================

找最小的数使得其他数都不是这个数的倍数，将所有数的约数删掉即可。

C. Chitauri \[jsy\]
===================

海盗分金，从后往前贪心

D. Dr. Banner \[zhy\]
=====================

递推，$f[i] = \sum_{j=1}^{i / 2}f[j]$，维护一下前缀和即可。

E. Egocentric Loki \[zxa\]
==========================

看了很久题目，很感动地看懂了题目是判断是否有点严格在某个三角形的外接圆中。

F. Fury \[jsy\]
===============

首先把所有的强联通分量找出来，连成一个环。剩下一个$DAG$，对于所有满足$a$可达$b$且不存在$c$使得$a$可达$c$且$c$可达$b$的$(a,b)$连一条$a$到$b$的有向边。

G. Groot \[jsy\]
================

看样例说话

H. Heimdall
===========

I. Iron Man \[jsy\]
===================

$F[i]$代表完成前$i$天任务的最小代价，并且第$i+1$天所有机器重新设置。转移就枚举从$i+1$到$j$天都使用同一种设置，然后代价可以用$KM$来算。

J. Jarvis \[jsy\]
=================

给每个顶点设定顶标，每条边的额外增加的权值为终点的顶标减去起点的顶标。判断是否有解就直接判断是否所有的环的权值和都为$0$，找方案直接$dfs$就好。

K. KSON \[zxa\]
===============

直接按照题意开涮，然而有key是空串的数据把我的写法炸飞了。
