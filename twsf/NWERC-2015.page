A. Assigning Workstations \[zxa\]
=================================

区间排序后贪个心

B. Better Productivity \[jsy\]
==============================

把区间分为大小区间，大区间定义为至少包含一个小区间的区间，小区间定义为不包含任何区间的区间。然后小区间直接dp很简单，大区间的话，一定是单独分成一组，或者和他所包含的那个小区间一组。

C. Cleaning Pipes \[zxa\]
=========================

线段相交后二分图染色

D. Debugging \[jsy\]
====================

设$f[i]$为：至多$i$行代码需要多少代价，$f[i] = min(f[i / j] + (j - 1) * r + p)$。这个直接枚举约数转移就可以了。

E. Elementary Math \[jsy\]
==========================

左图放方程，右图放数值，如果方程$x$能得到数值$y$，则$x$向$y$连边。右边最多有$3n$，所以直接跑最大匹配。

F. Flight Plan Evacuation \[jsy\]
=================================

计算航线和所有线段的交点，而海洋区域和陆地区域一定是交替出现的。题目保证了不会出现许多奇怪的情况，所以就很简单了。求交点：把经过各自线段的圆找出来，然后求一下旋转轴就得到了两个可能存在的交点，再判断一下就可以了。

G. Guessing Camels \[zxa\]
==========================

分治经典题

H. Hole in One \[zxa\]
======================

枚举所有排列，就能知道打击的方向，然后模拟是否合法。

I. Identifyng Map Tiles\[zhy\]
==============================

观察规律，可发现第i位是1对x的贡献是$2^{len-i-1}$，是2对y的贡献是$2^{len-i-1}$，是3对x和y都有$2^{len-i-1}$的贡献。

J. Jumbled Communication \[jsy\]
================================

按位模拟。

K. Kitchen Combinatorics\[zhy\]
===============================

读懂题意，暴力模拟即可。
