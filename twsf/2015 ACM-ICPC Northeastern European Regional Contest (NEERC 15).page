A. Adjustment Office \[zxa\]
============================

模拟，更新的时候维护一下行列之间互相影响的参数。

B. Binary vs Decimal \[jsy\]
============================

每个合法的数字去掉最高位之后还是合法的数字。所以通过在合法的数字前加$1$判断是否还是合法生产新的数字即可。

C. Cactus Jubilee \[zxa\]\[jsy\]
================================

先求出所有桥，计算出移动桥的方案数，再用并查集维护一下只保留桥的每个联通块大小，以及大小的$\sum{\binom{size}{2}}$。

然后考虑移动环上的边，实际上只需要使得新位置没有覆盖到任意一个环就行了，利用之前维护的东西很容易计算出这时的方案数。

D. Distance on Triangulation \[zxa\]\[jsy\]
===========================================

分治，每次找一条能让多边形分的尽可能平均的弦，然后用该弦的两个端点BFS，并更新所有跨过这条弦的询问的答案。该条弦把多边形分成了两个部分，递归下去即可。

以上的过程实际上就是在多边形三角剖分构成的树上进行边分治，因为每个点度数$\leq 3$，所以复杂度正确且只有一个$\log$。

E. Easy Problemset \[zxa\]
==========================

模拟。

F. Froggy Ford \[zxa\]
======================

新石子肯定放在两点中间，或者说是获得一次把某条边距离减半的机会。二分答案后用并查集check即可。

WA飞是因为用整数二分时距离爆了longlong。

G. Generators \[zhy\]
=====================

由于模数只有$1000$，所以对于第$j$个序列数列，处理出循环节后，问题变为给你$10000$个集合，每个集合最多$1000$个数，从每个集合取$1$个数，使得他们和最大，且不是$s$的倍数。贪心每个集合取最大的。如果是$s$的倍数的话，将一个集合中的最大数换成这个集合中与这个最大数不同余第二大的数更新答案即可。（换成同余的数肯定还是s的倍数，而换一个不同余的肯定不是$s$的倍数。）

H. Hypercube
============

I. Iceberg Orders
=================

J. Jump \[jsy\]
===============

先随机出n/2的答案。然后枚举位置$i$和位置$0$同时翻转，看答案是否改变，如果改变则和位置$1$不同真假，否则同真假。最后同时翻和$1$同真假的位置或者和$1$非同真假的位置check一下就行了。

K. King's Inspection \[jsy\]
============================

合并入度或者出度为1的节点，然后暴力。只要判个连通性就能过了。

L. Landscape Improved \[jsy\]
=============================

二分答案$H$，枚举拿个将要成为最高的那个位置$center$。找出最大的$L \leq i$
满足 $H - (center - L) \leq hL$，和最小的$R \geq i$满足
$H - (R - center) \leq hR$。然后把所有的物品都堆在$[L + 1, R - 1]$上，计算是否超过限制即可。
