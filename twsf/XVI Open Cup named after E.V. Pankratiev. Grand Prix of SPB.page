A. Bubbles \[zxa\]
==================

暴力求出所有中垂线和x轴的交点，交点分割出来的段数就是答案。

B. Drop7 \[zxa\]
================

大模拟，按照题意一步一步来。

C. Eulerian Graphs
==================

D. At Least Half \[jsy\] \[zhy\]
================================

枚举质因子$K$，把序列中所有是$K$倍数的数字替换成$1$，不是$K$倍数的数字换成$-1$。问题就转换成选择一个长度最长的连续子序列，使得子序列中的数字和不小于$0$。

最暴力的方法是枚举$O(n^2)$个区间可能的左端点$L$和右端点$R$。有两个性质是显然的：

-   如果存在$L' < L$且$sum[L'] \leq sum[L]$，那么就可以抛弃$L$作为左端点的可能。

-   如果存在$R' > R$且$sum[R'] \geq sum[R]$，那么就可以抛弃$R$作为右端点的可能。

删掉这些一定不可能的点后，枚举右端点，满足条件的最小左端点就具有了单调性，可以用单调队列$O(n)$来做了。

如果我们假设左右端点的数值都必须为$1$，求出若干个区间然后再贪心往左右扩展，那么就可以在$O(1的个数)$时间内完成任务。

总复杂度为：$O(质数个数之和)$

E. Next Partition in RLE Notation \[jsy\] \[zhy\]
=================================================

枚举一个数字$K$减一，然后看是否存在一组方案使得原来比$K$小的数字现在还比$K$小，比$K$大的不变。求出最小的$K$之后，后面的数字贪心填$K-1$，填到不能填为止。

F. Equation \[zxa\]
===================

首先满足阶数最小的$A \cdot X + B \cdot Y$是$X, Y$的最大公因式，只要利用辗转相除把最大公因式求出来就能得到一组$A,B$。

不用真做除法，有两个多项式$X , Y$，设$X$的阶数不超过$Y$的阶数，那就可以直接把$Y$乘个$x^k$使得$X,Y$同阶，再相减递归到$(Y, X - Y \cdot x^k)$就行，再维护一下系数的变化。

只要实现两个操作，一个是乘$x^k$，一个是求差，都可以压位，所以最后得到$O(\frac{n^2}{32})$的复杂度。

G. “Swap-Plus” Puzzle \[zxa\] \[jsy\]
=====================================

一定可以先交换行再交换列再交换格子，暴力枚举行列的变换然后统计。

H. Wrong Sieve \[zxa\]
======================

暴力每一轮后这个数的rank即可。

I. Space Cat \[jsy\]
====================

$f[i][0/1]$代表现在站在第$i$个平台且站在天花板(地板)所花费的最小能量，暴力转移。

J. Tic-Tac-Toe Variation\[zhy\]
===============================

游戏不存在平局，存在必胜策略，博弈搜索，每一步放在对方面临失败的点。胜态是指存在一种下法，使得对手是败态。不是胜态就是败态。

K. Captain Tarjan \[jsy\]
=========================

算出每条边被询问覆盖的次数，当作该边边权。设$F[i]$代表处理完第$i$棵子树重边所能覆盖的询问边数最大值。

$F[i] = \sum F[j] + max(w(i,j))$，$j$是$i$儿子。

然后通过换根就能算出每个节点为根的答案。

L. Gardening Lesson \[jsy\]
===========================

对于第一棵树，算出以每个点为根的树结构$Hash$值，存到$set$里面。

对于第二棵树，删掉一个叶子，算出以该叶子为根的树结构$Hash$值，到$set$里找有没有出现过这个$Hash$值，然后更新答案。

求$Hash$的过程相当于是一个$dp$的过程，所以可以通过换根来求这两个子问题。
