[Source](http://codeforces.com/gym/100553)

A. Alter Board \[jsy\]
======================

把偶数行和偶数列翻转。

B. Burrito King \[jsy\]
=======================

按照$\frac{ai}{bi}$从大到小排序，然后贪心放$xi$，能放则放。

C. Cactus Generator
===================

D. Damage Assessment \[jsy\]
============================

斜着用$simpson$积分，每次的$F$值都是一个圆的面积减去一个扇形的面积再加上一个三角形的面积。注意积分的右界应该是第一个不为$0$的$x$坐标，否则$simpson$会出问题。还有竖直的情况要注意一下。

E. Epic Win! \[jsy\]
====================

每个自动机节点存一个$bitset$代表当前对方可能在哪些节点上，设为可疑节点集合。

如果对方的所有节点的操作都是相同的，那么我们当前状态的操作就是必胜他的操作。如果他们节点的操作不完全相同，那么我们就随便出一个操作，根据他反馈的结果把可以节点集合分成三个部分。这样每种可疑节点集合之间的关系只可能是没有交集或者一个包含另一个，所以总节点数是$O(n)$级别的。

F. Filter \[zxa\]
=================

bitset模拟

G. Gomoku
=========

H. Hidden Maze
==============

I. Improvements \[jsy\]
=======================

先把序列表示成：$ai$代表第$i$个位置是标号为$ai$的船。

可以证明，答案是最长满足先递增后递减的子序列。

J. Jokewithpermutation \[jsy\]
==============================

$F[i][mask]$代表考虑了字符串的前$i$个字符，且排列中出现了数字集合$mask$是否可行。暴力$dp$即可。

K. Knockout Racing\[zxa\]
=========================

水题
