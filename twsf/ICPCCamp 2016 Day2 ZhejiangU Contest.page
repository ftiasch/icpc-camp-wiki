A. Alice's Sequence \[jsy\]
===========================

$c[i] = \sum_{j = 1}^{i} (a[i] + b[i])$，相当于$d[i] = \sum_{j = 1}^{i}b[j]$有个取值范围，我们的目标是让所有的$d[i]$尽可能相同，而且每个$d[i]$是独立的。所以可以贪心对取值范围取交，然后取到不可再取为止。

B. Bomb
=======

C. Clique \[jsy\]
=================

有一个$(n^3)$的方法是，
$O(n^2)$枚举机器人的终点的位置（不同的机器人的问题用容斥原理来解决），然后$O(n)$算每个机器人过去是否可行。如果把坐标按$(x,y)$旋转，那么行列就可以分离，这样位置就只有$O(n)$个了。

D. Deque and Balls \[jsy\]
==========================

有一个结论$x^{k} = \sum_{i=0}^k \binom{x}{i} \cdot S(k, i) \cdot i!$。可以用$dp$维护每个$S(k, i) \cdot i!$的系数。

设$dp[i][0/1][k][l]$代表考虑了前$i$个数字，并且双端队列两段是$k$和$i$，方向为$0/1$，$S(k, l) \cdot l!$的系数，实际上就是要利用
$\binom{x}{i}$推到$\binom{x+1}{i}$，这个部分可以利用$\binom{x + 1}{i} = \binom{x}{i} + \binom{x}{i - 1}$。

E. Easy When You Know How \[zxa\]
=================================

分块，暴力合并所有长度为$\sqrt{n}$的快。最后再把这些块暴力合并。

F. Four
=======

G. GCD is a Joke \[zxa\]
========================

定义好环上的模长，单位元和除法，就可以做欧几里得了。

H. Prefix Suffix \[zxa\]
========================

用后缀自动机求出S+T每个后缀能在S和T中匹配的长度，然后就变成了一个简单的二维区间询问问题。

I. Triple
=========

J. XOR Tree
===========
