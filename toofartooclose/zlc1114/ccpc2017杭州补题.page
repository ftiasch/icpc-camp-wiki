A.
==

B.
==

C. 
==

D.
==

E. Master of Subgraph
=====================
题意：给定一颗树，问你其中的联通子图能组成 $1$ 到 $m$ 的数字么

做法：树分治后变成一个树形依赖背包，然后 $dfs$ 的时候 $DP$ 即可

F. Hearthrock \[待补\]
=======================
题意: 炉石传说，可能大模拟



G. Marriage
==================

题意：$n (1e5)$ 个家庭，给出每个家庭人数，每个家庭内部不能通婚，问你有多少种配种方式

做法：考虑容斥，假设算出了至少 $k$ 对骨科的方案数，每个家庭是个多项式，然后分治 $fft$ 后容斥一下就做完了

H. Master of Connected Component
=====================

题意：给你两颗树$(n<=1e4)$，树上有另外一个图的边，问你第一第二颗树根到 $i$ 的节点上的边都加上以后，有几个联通快

我实在是太菜了，看了题解又问 $tls$ 做法

我是对于每棵树按高度分块，然后对于每对块（一个树最多$\sqrt(n)$个块，总共最多 $\sqrt(n)*\sqrt(n)=n$ 组），把询问放到块上

每个询问最多往上找 $\sqrt(n)$ 长度，每个块由上面那个块得到

相当于一共询问 $n+n$ 次，直接并查集然后回滚到历史状态即可

I. Master of Matrix
============================

题意：给你个图（$n<=25,m<=25$），相邻两个点可能满足和小于某值或大于某值，你花费 $w[i][j]$ 使得 $a[i][j]/=2$ ，问你满足条件最小代价

做法：奇偶染色后最小割

建图方法：把 $x,x/2...$ 放到边上，连 $INF$ 边的本质是去掉块到块的最小割！最小割是使得 $S$ 和 $T$ 不连通！

J. 
==============================================

K.
===================

# L. Mod, Xor and Everything

题意：$(n\%1) \bigoplus (n\%2) \bigoplus ... \bigoplus (n\%n)$

做法：按 $n/i$ 的值分类然后会发现每块都是等差数列

等差数列异或和可以通过按位来算加上类欧几里得来做

需要注意如果数量比较小，最好直接暴力得到值，因为$两个log会被卡=_=！$

orz我还被卡的去问了tls qwq

Summary
=======
我好菜菜啊.jpg

