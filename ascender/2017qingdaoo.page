A. Apple \[zmy\]
================

用行列式求出外接圆心，然后用高精度判断点是否在圆外。

B. Bomberman
============

C. The Dominator of Strings \[zmy\]
===================================

挑选最长串建立后缀自动机，让其他所有串在后缀自动机上跑一遍即可。

D. The Intersection \[zmy\]
===========================

我们在Stern-Brocot
tree上从根往下走，每次判断应该向右还是向左，类似于二分。当分母大于$100000$时停止。对此时的左右端点再进行判断，即可得到答案。这里需要用
`__int128_t`

E. King's Visit
===============

F. Pythagoras \[zmy\]
=====================

我们先利用Tree of primitive Pythagorean triples
预处理，然后暴力计算。注意几个常数优化的地方：

-   取模使用 &
-   判断是否大于inf时使用int
-   最大的数一定是第三个
-   计算转移时预处理$x<<1,y<<1,z<<1,z*3$的值
-   快速读入

G. Zuma
=======

H. Chinese Zodiac \[zmy\]
=========================

模拟

I. Smallest Minimum Cut \[zmy\]
===============================

边权设为$2000*2+1$跑dinic就行。

J. Brute Force Sorting \[jds\]
==============================

每轮删数只会删上轮旁边的数，并且旁边也一定有个数被删，所以假如第一轮删k个，后面每轮删的数只会越来越少，所以每个数最后只有可能被删一次或多次，而每多可能被删一次，就代表它上次必定没被删，没被删则$k$会减$1$，所以最终操作最多$n+k$次，模拟的删便是$\mathcal{O}(n)$的复杂度。

K. A Cubic number and A Cubic Number \[zmy\]
============================================

因为要表示的数是一个质数，所以一定是连续的两个数的立方差，预处理下就行了。

总结反思
========

### replay

开局zmy上去写H，结果样例都没过就交了，WA1。wbr看了眼代码发现zmy减反了，改到以后就A了。然后wbr想出了K，zmy上去1A了。zmy和wbr想出了09，zmy上去1A了。jds想出了03，zmy上去1A了。然后jds开始写10，zmy和wbr研究01如何写简单。jds
WA了4发过了10，中间zmy也写好，但是又WA了一发01。经过三次改动终于过掉了01。然后wbr
想到用连分数去搞04，先是T了。把递归深度调低以后，又WA了。然后zmy和wbr就陷入了04的无尽深渊。期间zmy还去确定了下：“一个有理分数p/q被称之为实数x的最佳有理逼近,当且仅当在所有分母不超过q的有理分数中,|p/q-x|最小”。却仍然没意识到错，反而更加坚定的使用连分数来搞。jds一直在想07.到最后半个小时交了一发07，T了。更改无果，打出GG。

**zmy** 04太蠢了，竟然没想到 stern-brocot tree
，之前做过一道cf题也用到了这个，但是没仔细研究。看来要好好复习下原来学过的东西。此外，开题以后要适时放弃，从最后的角度来讲，三人一起搞07也许更靠谱一些。

**jds** 10写的时候有几个小bug，实在是不应该。。。

**wbr**
