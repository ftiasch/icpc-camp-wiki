维护一个BASE\*BASE的矩阵表示当前线性基。每次插入一个数，从高到低依次判断其当前位是否有一个基，如果有，消去并continue，否则插入其中并break。

-   `bzoj4269`
    求出线性基，最大值为所有基底异或起来。次大值为最大值异或一个最小的基。

-   `hdu3949`
    先求出线性基，如果这n个数线性相关，那么k--。之后看k的每一位是否为1，异或相应的基即可。

-   `bzoj2115`
    任意找一条从1到n的路径。不难得出，1所在联通块中任意一个环都可以异或到路径上去，所以我们找出所有环，求出线性基，然后贪心即可。

-   `bzoj2844`
    求出线性基。考虑每个数字出现的次数，应该为2\^(n-basis\_cnt)次。

-   `bzoj2460` 按价值排序，从大到小，贪心地添加数字。

-   `bzoj4004` 按价值排序，从小到大，贪心地添加数字。

-   `bzoj4568`
    求出树上倍增的线性基。查询的时候，先求出lca，然后利用ST表的查询思想，仅需合并4次线性基。


