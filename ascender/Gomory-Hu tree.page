每次对于当前点集选定两个顶点分别作为源点和汇点，然后求解最小割。这样所有点就被划分成了两个集合：与S相连、不与S相连。递归下去建树即可。

- `bzoj2229` 模板题
- `bzoj4519` 模板题
- `bzoj4435` 如果使用之前的方式暴力更新点对间最小割$O(n^3)$是不行的，所以这里用hash来记录点被分割的情况。计算答案的时候，枚举点对，然后先判断被$0$分割的情况，如果情况不同则最小割为$0$，否则再判断被$1$分割的情况，以此类推。