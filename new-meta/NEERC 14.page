G.Gomuku
========

**题意:**有个五子棋AI, 他会计算所有连续五个格子里面的分数和:
如果连续五格里只有他的棋子, 有$k$个, 局面得分加$50^{2k-1}$分,
如果只有对手的棋子, 有k个,局面得分减$50^{2k}$分.
如果双方的棋子都有或都没有, 不算分. 最后他会加一个$[0,2499]$的随机数.
他每次会选择使得局面得分最大的一手. 现在他第一步下在棋盘正中,
要你做他的对手, 打100盘赢100盘.

**做法:**硬想策略是比较累的. 把AI模拟出来, 和他下几盘棋,
下赢了把策略输出即可. 注意有随机, 有的策略是确定的,
有的策略可能受到随机数略微影响.

[code](NEERC14G)

H.Hidden Maze
=============

**题意: ** 给你一棵随机生成的树(给$\binom{n}{2}$条边一个随机排列,
然后for一遍连接没有连接的点). 每条边上有个权值.
问所有长度为奇数的路径上的权值中位数的平均值.

**做法: ** 首先假设每条边上的权值各不相同.
一条路径上的权值中位数是$s$的条件是,
这条路径上大于$s$的边数等于小于$s$的边数. 所以我们枚举中位数在哪条边上,
然后两边扩展.
只要两边(大于$s$的变数和小于$s$的边数的差)加起来等于0就是一组合法解(注意此时路径长度也自然为奇数了).
因为树是随机生成的, 所以不会太深. 所以我们从小到大枚举边的话,
就能快速维护两边的扩展情况. 总复杂度$\sum_{v\in V}h(v)dep(v)$,
$h(v)$为点$v$的高度(即$v$和他最深的子孙的距离), $dep(v)$为$v$的深度.

[code](NEERC14H)
