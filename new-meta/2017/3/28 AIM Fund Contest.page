B. Bus Stop
===========

**题意：**

给你$n\leq 1e5$个随机变量，每个变量在$[0,t_i]$中随机分布，求这$n$个变量的最小值的期望。

**题解：**

直接积分的话，由于有些项是负的，有些项是正的，所以中间过程精度爆炸。可以这样算：设$P_k$是有$k$个变量落进了最小区间$[0,t_{min}]$的概率，那么答案是$\sum_{k=0}^nP_k\cdot \frac{t_{min}}{k+1}$，而$P_k$是$\prod (\frac{t_{min}}{t_i}x+\frac{ti-t_{min}}{ti})$中$x^k$的系数，可以用分治FFT计算。

[B.cpp](2017/3/28%20AIM%20Fund%20Contest%20B.cpp)

D. Decomposition
================

**题意: **

定义两个无向简单图$G_1=(V_1,E_1)$和$G_2=(V_2,E_2)$的叉积为$G_1\times G_2=(V_1\times V_2, \{\{(u,v),(u',v')\}|(u=u'且\{v,v'\}\in E_2)或(v=v'且\{u,u'\}\in E_1)\})$.
现在要你把输入的一个图$n,m\le 200000$分解为一个图和一个尽量大的hypercube的叉积.
(大小为$k$的hypercube是$(\{0,1\}^k,\{(u,v)|u和v的\text{hamming distance}为1\})$.)

**做法: **

首先观察发现定义的叉积有结合律,
大小为$k$的hypercube是由$k$个大小为$1$的hypercube叉积得到的,
以及大小为$1$的hypercube(即($\{0,1\},\{\{0,1\}\}$))可以和任何图$G$交换.
所以我们每次尝试分解出一个大小为$1$的hypercube就好了.
一个图和一个大小为$1$的hypercube的叉积就是把图复制一份,
再在两份中对应点之间连边. 所以我们找到度数最小的点,
枚举他的邻居中谁是他另一份的对应点, 从这个开始就可以线性check可行性了.
根据鸽笼原理, 一次分解的复杂度是$m/n\times m$=$O(m\sqrt{m})$.
每次边数至少减半, 所以总复杂度也是$O(m\sqrt{m})$.

F. Sum of divisors
==================

**题意:**

给你一个$n$，这个$n$称之为$k-expressible$当且仅当它可以表示成恰好$k$个自己的约数的和。现在有5w个询问，每个询问问$[a, b]$中有多少个$k-expressible$的数字。

**题解:**

如果一个数字能这么表示，肯定是可以写成$1 = \frac{1}{x_1} + \frac{1}{x_2} + \cdots + \frac{1}{x_k}$。把这样的pattern都搜出来，然后把lcm成倍数关系的去掉，可以发现每个k对应的lcm集合最大才15，而且他们的共性在于任意一个集合的lcm的不同个数只有2000级别。

那么先处理出来这2000个不同lcm的容斥系数，每个询问直接for一遍就好。

[AIM Fund Contest F.cpp]()
